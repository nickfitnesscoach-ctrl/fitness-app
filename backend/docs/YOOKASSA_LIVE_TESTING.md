# Тестирование Live-платежей YooKassa

## Обзор

Для проверки корректности интеграции с **боевым магазином YooKassa** реализован специальный тестовый тариф за **1₽**.

Этот тариф:
- ✅ Использует **реальные credentials** боевого магазина
- ✅ Создаёт **реальный платёж** (списание с реальной карты)
- ✅ Получает **реальные webhooks** от YooKassa
- ✅ Обновляет **реальные записи** в БД
- ⚠️ **Доступен ТОЛЬКО админам** (проверка по `TELEGRAM_ADMINS`)

---

## Предварительные требования

### 1. Получить боевые credentials YooKassa

1. Войти в https://yookassa.ru/
2. Перейти: **Настройки → Интеграция**
3. Скопировать:
   - **shopId** (идентификатор магазина)
   - **Секретный ключ** (начинается с `live_`)

### 2. Настроить .env на production сервере

```bash
# Подключиться к серверу
ssh root@85.198.81.133

# Открыть .env
cd /opt/foodmind/backend
nano .env
```

Добавить/обновить:

```ini
# YooKassa Live Credentials
YOOKASSA_MODE=prod
YOOKASSA_SHOP_ID_PROD=ваш_shop_id
YOOKASSA_API_KEY_PROD=live_ваш_секретный_ключ

# Admins (ваш telegram_id)
TELEGRAM_ADMINS=310151740
```

**ВАЖНО:** Никогда не коммитить `.env` с live credentials в Git!

---

## Пошаговая инструкция тестирования

### Шаг 1: Применить миграции

```bash
# На сервере
ssh root@85.198.81.133
cd /opt/foodmind

# Применить миграцию с тестовым тарифом
docker exec fm-backend python manage.py migrate billing

# Проверить, что тариф создан
docker exec fm-backend python manage.py shell
>>> from apps.billing.models import SubscriptionPlan
>>> SubscriptionPlan.objects.filter(name='TEST_LIVE', is_test=True)
<QuerySet [<SubscriptionPlan: Test Live Payment 1₽ - 1.00₽>]>
>>> exit()
```

### Шаг 2: Перезапустить backend

```bash
# Перезапустить backend для применения новых env variables
cd /opt/foodmind
docker-compose restart backend

# Проверить логи
docker logs fm-backend --tail=50 | grep YOOKASSA
# Должно быть: YooKassa client initialized. Shop ID: xxx, Mode: PRODUCTION
```

### Шаг 3: Открыть мини-ап под админом

1. Открыть Telegram
2. Запустить бота EatFit24
3. Открыть WebApp
4. Убедиться, что авторизация прошла (ваш telegram_id в `TELEGRAM_ADMINS`)

### Шаг 4: Найти кнопку тестового платежа

1. Перейти: **Профиль** → **Настройки** (иконка шестеренки)
2. Нажать: **Подписка и оплата**
3. Прокрутить вниз
4. Увидеть **желтый блок** с кнопкой:
   ```
   🧪 Тестирование Live-платежей
   Проверка боевого магазина YooKassa. Платёж 1₽ на реальную карту.

   [Тест: Оплатить 1₽ (live)]
   ```

**Если кнопки НЕТ:**
- Проверить, что вы авторизованы под админом
- Проверить логи backend: `docker logs fm-backend | grep is_admin`
- Убедиться, что ваш telegram_id в переменной `TELEGRAM_ADMINS`

### Шаг 5: Запустить тестовый платёж

1. Нажать кнопку **"Тест: Оплатить 1₽ (live)"**
2. Откроется платёжная форма YooKassa
3. Ввести данные **реальной банковской карты**:
   - Номер карты
   - Срок действия
   - CVV
4. Подтвердить платёж (может прийти SMS с кодом)
5. Дождаться успешной оплаты

### Шаг 6: Проверить webhook

```bash
# На сервере - следить за логами в реальном времени
docker logs fm-backend -f | grep -E "payment.succeeded|webhook|TEST_LIVE"

# Должны увидеть:
# INFO Webhook received from authorized IP: 185.71.76.X
# INFO Payment succeeded: payment_id=XXX, status=succeeded
# INFO Payment 1₽ (TEST_LIVE) marked as succeeded for user ...
```

### Шаг 7: Проверить обновление в БД

```bash
# Зайти в Django shell
docker exec -it fm-backend python manage.py shell

# Проверить последний платёж
from apps.billing.models import Payment
last_payment = Payment.objects.filter(
    plan__name='TEST_LIVE'
).order_by('-created_at').first()

print(f"Payment ID: {last_payment.id}")
print(f"Status: {last_payment.status}")  # Должен быть 'succeeded'
print(f"Amount: {last_payment.amount}")  # Должен быть 1.00
print(f"YooKassa ID: {last_payment.yookassa_payment_id}")
```

### Шаг 8: Проверить UI

1. Вернуться в мини-ап
2. Обновить страницу (свайп вниз)
3. Проверить, что:
   - В разделе "Подписка и оплата" появился новый статус
   - В истории платежей появилась запись "1₽ - Test Live Payment"

---

## Troubleshooting

### Ошибка: "Доступ запрещён: только для админов"

**Причина:** Ваш telegram_id отсутствует в `TELEGRAM_ADMINS`

**Решение:**
```bash
# 1. Узнать свой telegram_id
# В боте отправить /start и посмотреть логи:
docker logs fm-backend | grep "telegram_id"

# 2. Добавить в .env
nano /opt/foodmind/backend/.env
TELEGRAM_ADMINS=310151740,ваш_telegram_id

# 3. Перезапустить backend
docker-compose restart backend
```

### Ошибка: "Тестовый план не найден"

**Причина:** Миграция не применена

**Решение:**
```bash
docker exec fm-backend python manage.py migrate billing
docker-compose restart backend
```

### Ошибка: "YooKassa API error: 401 Unauthorized"

**Причина:** Неверные credentials

**Решение:**
1. Проверить, что `YOOKASSA_MODE=prod`
2. Проверить, что `YOOKASSA_SHOP_ID_PROD` и `YOOKASSA_API_KEY_PROD` корректны
3. Убедиться, что `YOOKASSA_API_KEY_PROD` начинается с `live_`
4. Перезапустить backend после изменения .env

### Webhook не приходит

**Причина:** Не настроен webhook URL в YooKassa

**Решение:**
1. Войти в https://yookassa.ru/
2. Перейти: **Настройки → Уведомления**
3. Добавить webhook URL:
   ```
   https://eatfit24.ru/api/v1/billing/webhooks/yookassa
   ```
4. Выбрать события:
   - `payment.succeeded`
   - `payment.canceled`
   - `refund.succeeded`
5. Сохранить

---

## Возврат в тестовый режим

После завершения тестирования **обязательно** вернуть режим test:

```bash
# На сервере
nano /opt/foodmind/backend/.env

# Изменить:
YOOKASSA_MODE=test

# Перезапустить
docker-compose restart backend
```

---

## Безопасность

### ⚠️ ВАЖНО

1. **НЕ коммитить** live credentials в Git
2. **НЕ показывать** скриншоты с secretKey
3. **Ограничить доступ** к production .env файлу
4. **Использовать** тестовый режим для разработки
5. **Мониторить** логи при тестировании live-платежей

### Проверка доступа

Endpoint `POST /api/v1/billing/create-test-live-payment/` защищён:

1. ✅ Требует аутентификации (`@permission_classes([IsAuthenticated])`)
2. ✅ Проверяет наличие `telegram_user`
3. ✅ Проверяет telegram_id в `settings.TELEGRAM_ADMINS`
4. ✅ Логирует все попытки доступа
5. ✅ Возвращает 403 Forbidden для не-админов

---

## Дальнейшие шаги

После успешного тестирования:

1. ✅ Убедиться, что webhooks работают
2. ✅ Проверить, что БД обновляется корректно
3. ✅ Протестировать полный цикл подписки:
   - Оплата → Активация → Автопродление → Webhook → Обновление UI
4. ✅ Настроить мониторинг платежей в production
5. ✅ Вернуть `YOOKASSA_MODE=test` для dev-окружения

---

## Полезные ссылки

- [Документация YooKassa API](https://yookassa.ru/developers/api)
- [Тестовые данные YooKassa](https://yookassa.ru/developers/using-api/testing)
- [Webhooks YooKassa](https://yookassa.ru/developers/using-api/webhooks)
- [Django Admin](https://eatfit24.ru/admin/billing/subscriptionplan/)

---

**Автор:** Claude Code
**Дата:** 2025-01-28
**Версия:** 1.0
