# Промпт для AI (Anthropic Claude / Google Gemini)

## Задача
Создать Telegram Mini App для FoodMind AI - приложения для трекинга питания с AI рекомендациями.

---

## Технологический стек

**Frontend:**
- React 18 + TypeScript
- Vite
- Chart.js + react-chartjs-2 (для графиков КБЖУ)
- react-markdown (для отображения AI планов)
- Telegram WebApp SDK

**Backend (уже готов):**
- Django REST Framework
- PostgreSQL
- JWT аутентификация

---

## Структура данных (из API)

### 1. Аутентификация

```typescript
// POST /api/v1/telegram/auth/
// Headers: { "X-Telegram-Init-Data": "<initData from Telegram.WebApp.initData>" }

interface AuthResponse {
  access: string;        // JWT токен
  refresh: string;       // Refresh токен
  user: {
    id: number;
    username: string;
    telegram_id: number;
    first_name: string;
    completed_ai_test: boolean;  // ВАЖНО: показывает прошел ли юзер тест
  };
}
```

### 2. Профиль пользователя

```typescript
// GET /api/v1/telegram/profile/
// Headers: { "Authorization": "Bearer <token>" }

interface TelegramProfile {
  telegram_id: number;
  first_name: string;
  last_name?: string;
  ai_test_completed: boolean;  // ВАЖНО: если false - показать кнопку "Пройти тест"

  // КБЖУ (назначается тренером после прохождения теста)
  assigned_calories?: number;    // может быть null, если еще не назначено
  assigned_protein?: number;
  assigned_fat?: number;
  assigned_carbs?: number;
}
```

### 3. Расширенный профиль (опционально)

```typescript
// GET /api/v1/users/profile/

interface UserProfile {
  // Базовые данные
  gender: 'M' | 'F';
  weight: number;
  height: number;
  activity_level: 'sedentary' | 'lightly_active' | 'moderately_active' | 'very_active';
  goal_type: 'weight_loss' | 'maintenance' | 'weight_gain';

  // КБЖУ и план (назначаются тренером)
  assigned_calories?: number;
  assigned_protein?: number;
  assigned_fat?: number;
  assigned_carbs?: number;
  trainer_plan?: string;         // Markdown текст плана от тренера
}
```

---

## UI/UX Требования

### Экран 1: Загрузка (пока идет аутентификация)
- Показать loading spinner
- Автоматически аутентифицировать через Telegram WebApp

### Экран 2: Тест не пройден (`ai_test_completed: false`)
```
┌─────────────────────────────┐
│  Добро пожаловать! 👋       │
│                             │
│  Для начала работы пройдите │
│  AI тест в нашем боте       │
│                             │
│  [Пройти AI тест]           │
└─────────────────────────────┘
```
Кнопка открывает: `https://t.me/AI_test_bot`

### Экран 3: Тест пройден, ожидание назначения (`ai_test_completed: true`, `assigned_calories: null`)
```
┌─────────────────────────────┐
│  Привет, Николай! 👋        │
│                             │
│  Тренер скоро назначит вам  │
│  персональный план питания  │
│                             │
│  🔄 Ожидание...             │
└─────────────────────────────┘
```

### Экран 4: Дашборд (тест пройден + КБЖУ назначено)
```
┌─────────────────────────────┐
│  Привет, Николай! 👋        │
│                             │
│  Ваш план питания           │
│  ┌─────────────────────┐    │
│  │      2100 ккал      │    │
│  │                     │    │
│  │   [Круговая         │    │
│  │    диаграмма]       │    │
│  │                     │    │
│  │  Белки: 130г        │    │
│  │  Жиры: 70г          │    │
│  │  Углеводы: 240г     │    │
│  └─────────────────────┘    │
│                             │
│  План от тренера 👨‍⚕️        │
│  ┌─────────────────────┐    │
│  │ [Текст плана        │    │
│  │  от тренера         │    │
│  │  в Markdown]        │    │
│  └─────────────────────┘    │
└─────────────────────────────┘
```

---

## Компоненты для реализации

### 1. AuthProvider (Context)
- Аутентификация через Telegram WebApp при загрузке
- Сохранение JWT токена в localStorage
- Предоставление user объекта всему приложению

### 2. Dashboard Component
- Проверяет `ai_test_completed` и `assigned_calories`
- Если `!ai_test_completed` → показывает кнопку "Пройти тест"
- Если `ai_test_completed && !assigned_calories` → показывает "Ожидание назначения от тренера"
- Если `ai_test_completed && assigned_calories` → показывает дашборд с КБЖУ + план

### 3. MacroChart Component
- Принимает: `{ calories, protein, fat, carbs }`
- Отображает круговую диаграмму (Chart.js Doughnut)
- Показывает числа под диаграммой

### 4. TrainerPlan Component
- Получает `trainer_plan` (Markdown текст)
- Рендерит Markdown в HTML
- Показывает план от тренера

---

## Telegram WebApp Integration

```typescript
// Инициализация
useEffect(() => {
  const tg = window.Telegram?.WebApp;
  if (tg) {
    tg.ready();
    tg.expand();  // Раскрыть на весь экран

    // Применить тему Telegram
    document.documentElement.style.setProperty(
      '--tg-theme-bg-color',
      tg.themeParams.bg_color || '#ffffff'
    );
  }
}, []);

// Аутентификация
const initData = window.Telegram?.WebApp?.initData;
// Отправить в POST /api/v1/telegram/auth/

// Открыть бота
window.Telegram.WebApp.openTelegramLink('https://t.me/AI_test_bot');
```

---

## Обработка состояний

### Loading
```typescript
if (loading) return <div>Загрузка...</div>;
```

### Error
```typescript
if (error) return <div>Ошибка: {error}</div>;
```

### Тест не пройден
```typescript
if (!profile.ai_test_completed) {
  return <TestPrompt />;
}
```

### Дашборд
```typescript
return (
  <Dashboard>
    <MacroChart {...macros} />
    <AIRecommendations />
  </Dashboard>
);
```

---

## Цветовая схема

Использовать Telegram theme colors:
- Background: `tg.themeParams.bg_color`
- Text: `tg.themeParams.text_color`
- Button: `tg.themeParams.button_color`
- Link: `tg.themeParams.link_color`

Для круговой диаграммы:
- Белки: синий `rgba(54, 162, 235, 0.8)`
- Жиры: желтый `rgba(255, 206, 86, 0.8)`
- Углеводы: зеленый `rgba(75, 192, 192, 0.8)`

---

## Файловая структура

```
src/
├── components/
│   ├── Dashboard.tsx          # Главный экран
│   ├── MacroChart.tsx         # Круговая диаграмма КБЖУ
│   ├── AIRecommendations.tsx  # AI план
│   └── TestPrompt.tsx         # Призыв пройти тест
├── contexts/
│   └── AuthContext.tsx        # Аутентификация
├── hooks/
│   └── useProfile.ts          # Хук для получения профиля
├── services/
│   └── api.ts                 # API клиент
├── types/
│   └── telegram.d.ts          # TypeScript декларации
├── App.tsx                    # Точка входа
└── App.css                    # Стили
```

---

## Критически важно

1. **Проверять `ai_test_completed`** перед показом дашборда
2. **Использовать Telegram WebApp SDK** для:
   - Получения `initData` для аутентификации
   - Открытия ссылок (`openTelegramLink`)
   - Применения темы приложения
3. **Сохранять JWT токен** в localStorage
4. **Обрабатывать ошибки** (401 → повторная аутентификация)

---

## API Endpoints (краткая справка)

```
POST   /api/v1/telegram/auth/           → Аутентификация
GET    /api/v1/telegram/profile/        → Базовый профиль + КБЖУ
GET    /api/v1/users/profile/           → Расширенный профиль + AI план
```

---

## Примеры данных

### Пользователь не прошел тест
```json
{
  "ai_test_completed": false,
  "recommended_calories": null,
  "recommended_protein": null,
  "recommended_fat": null,
  "recommended_carbs": null
}
```

### Пользователь прошел тест
```json
{
  "ai_test_completed": true,
  "recommended_calories": 2100,
  "recommended_protein": 130,
  "recommended_fat": 70,
  "recommended_carbs": 240
}
```

---

## Итоговый промпт для AI

> Создай Telegram Mini App на React + TypeScript для FoodMind AI.
>
> **Требования:**
> 1. Аутентификация через Telegram WebApp (POST /api/v1/telegram/auth/)
> 2. Если `ai_test_completed: false` → показать кнопку "Пройти AI тест" (открывает @AI_test_bot)
> 3. Если `ai_test_completed: true` → показать дашборд с:
>    - Круговой диаграммой КБЖУ (Chart.js)
>    - AI планом в Markdown
>    - Кнопкой "Обновить план"
> 4. Использовать Telegram theme colors
> 5. Адаптивный дизайн для мобильных устройств
>
> **API данные:** см. FRONTEND_API_SCHEMA.md
> **Примеры кода:** см. FRONTEND_REACT_EXAMPLES.md

---

**Версия:** 3.1.1
**Дата:** 2025-11-20
**Проект:** FoodMind AI
