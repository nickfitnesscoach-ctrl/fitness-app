# Техническое задание: Аудит фронтенда EatFit24
## Полный анализ критических багов и roadmap устранения

**Дата:** 2025-12-06
**Исполнитель:** Droid (Frontend AI Agent)
**Приоритет:** КРИТИЧЕСКИЙ
**Срок:** 2-3 дня

---

## 🎯 Цель аудита

Провести полный аудит фронтенда приложения EatFit24 с фокусом на выявление и устранение критических багов, особенно связанных с асинхронным распознаванием фотографий AI.

---

## 🔴 КРИТИЧЕСКИЙ БАГ #1: "Еда не найдена" при успешном распознавании

### Описание проблемы
Пользователь отправляет фото еды → Backend успешно распознает продукты → Фронтенд показывает ошибку "Еда не найдена"

### Данные от Backend (подтверждено логами)
```
✅ Meal 59: 2 продукта распознано за 25.66s
   - Фузилли (варёные)
   - Куриные крылышки (запечённые/жареные, с кожей)

✅ Meal 58: 2 продукта распознано за 32.71s
   - паста (фузилли), варёная
   - куриные крылышки, запечённые/жареные с кожей

✅ Meal 57: 3 продукта распознано за 26.33s
   - банан (целый, со шкуркой)
   - кофе с молоком (чашка)
   - мини-пирожное/корзиночка с джемом
```

### Требуется проверить

#### 1. Асинхронный flow распознавания
**Путь:** Проверить весь цикл async AI recognition

**Backend API endpoints:**
- `POST /api/v1/ai/recognize/` → возвращает `{meal_id, task_id, status: "processing"}`
- `GET /api/v1/ai/task/<task_id>/` → polling статуса задачи

**Что проверить:**
- [ ] Фронтенд правильно обрабатывает HTTP 202 Accepted?
- [ ] Реализован ли polling endpoint `/api/v1/ai/task/<task_id>/`?
- [ ] Интервал polling (должен быть 2-3 секунды)
- [ ] Timeout polling (максимум 60 секунд)
- [ ] Обработка состояний: `pending`, `processing`, `success`, `failed`
- [ ] Парсинг ответа с `recognized_items` массивом

**Файлы для проверки:**
```
frontend/src/services/aiService.ts (или .js)
frontend/src/hooks/useAIRecognition.ts
frontend/src/pages/FoodLogPage/* (компонент загрузки фото)
frontend/src/api/ai.ts
```

#### 2. Структура данных API response
**Ожидаемый ответ от `/api/v1/ai/task/<task_id>/`:**
```typescript
{
  "success": true,
  "meal_id": "57",
  "recognized_items": [
    {
      "id": "uuid",
      "name": "банан (целый, со шкуркой)",
      "grams": 150,
      "calories": 133,
      "protein": 1.6,
      "fat": 0.5,
      "carbohydrates": 32.8,
      "confidence": 0.95
    },
    // ... more items
  ],
  "totals": {
    "calories": 450,
    "protein": 12.5,
    "fat": 15.2,
    "carbohydrates": 65.3
  },
  "recognition_time": 26.33
}
```

**Что проверить:**
- [ ] Фронтенд ожидает правильную структуру данных?
- [ ] Поля `recognized_items` vs `food_items` (возможно несоответствие)
- [ ] TypeScript интерфейсы соответствуют API?
- [ ] Обработка пустого массива `recognized_items: []`

#### 3. Error handling
**Сценарии ошибок для проверки:**
- [ ] Task не найден (404)
- [ ] Task failed с ошибкой AI
- [ ] Timeout polling (задача > 60 секунд)
- [ ] Network error во время polling
- [ ] Пустой результат распознавания

**Что проверить:**
- [ ] Показывается ли правильное сообщение об ошибке?
- [ ] "Еда не найдена" - где генерируется это сообщение?
- [ ] Fallback на синхронный режим, если async failed?

#### 4. State management
**Что проверить:**
- [ ] Состояние загрузки (`isLoading`, `isProcessing`)
- [ ] Кэширование результатов (может показывать старый результат)
- [ ] Очистка состояния перед новым запросом
- [ ] Race conditions при быстрых повторных запросах

---

## 📋 ПОЛНЫЙ СПИСОК ПРОВЕРОК АУДИТА

### A. Критические функциональные баги

#### A1. AI Распознавание (КРИТИЧНО)
- [ ] Async recognition flow с polling
- [ ] Sync recognition fallback
- [ ] Обработка успешного результата
- [ ] Обработка пустого результата
- [ ] Отображение progress bar / loader
- [ ] Retry механизм при network errors

#### A2. Навигация и роутинг
- [ ] Переходы между страницами
- [ ] Deep links работают корректно
- [ ] Back button сохраняет состояние
- [ ] Передача параметров между страницами (например, selectedDate)

#### A3. Аутентификация через Telegram
- [ ] Telegram WebApp initData валидация
- [ ] JWT token сохранение и обновление
- [ ] Header `X-Telegram-Init-Data` отправляется
- [ ] Logout очищает все данные
- [ ] Redirect на /login при 401

#### A4. Работа с датами
- [ ] Выбор даты в календаре
- [ ] Отображение приёмов пищи за выбранную дату
- [ ] Timezone handling (UTC vs local time)
- [ ] Граничные даты (прошлое/будущее)

#### A5. CRUD операции с приёмами пищи
- [ ] Создание нового meal
- [ ] Редактирование существующего meal
- [ ] Удаление meal
- [ ] Добавление/удаление food items
- [ ] Изменение граммов продуктов
- [ ] Автопересчёт КБЖУ

### B. UI/UX баги

#### B1. Отображение данных
- [ ] Пустые состояния (no data)
- [ ] Loading состояния
- [ ] Error states с читаемыми сообщениями
- [ ] Форматирование чисел (calories, macros)
- [ ] Truncate длинных названий продуктов

#### B2. Формы и инпуты
- [ ] Валидация полей
- [ ] Сообщения об ошибках
- [ ] Disabled состояния кнопок
- [ ] Клавиатура на мобильных (numeric для чисел)

#### B3. Responsive дизайн
- [ ] Мобильная версия (< 768px)
- [ ] Планшет (768-1024px)
- [ ] Desktop (> 1024px)
- [ ] Telegram WebApp viewport
- [ ] Safe area для iPhone notch

### C. Производительность

#### C1. Network запросы
- [ ] Дублирование запросов
- [ ] Отсутствие дебаунса в search
- [ ] Лишние re-renders
- [ ] Cache-Control headers используются?
- [ ] Request cancellation при unmount

#### C2. Рендеринг
- [ ] Мемоизация компонентов (React.memo)
- [ ] useMemo / useCallback где нужно
- [ ] Виртуализация длинных списков
- [ ] Lazy loading изображений

#### C3. Bundle size
- [ ] Неиспользуемые зависимости
- [ ] Code splitting
- [ ] Tree shaking работает
- [ ] Проверить размер bundle (должен быть < 500KB gzipped)

### D. Безопасность

#### D1. XSS защита
- [ ] Sanitization пользовательского ввода
- [ ] Опасное использование `dangerouslySetInnerHTML`
- [ ] Валидация файлов (фото)

#### D2. Sensitive data
- [ ] Токены не логируются в console
- [ ] localStorage vs sessionStorage
- [ ] Очистка данных при logout

### E. Консистентность данных

#### E1. Синхронизация с backend
- [ ] Optimistic updates работают корректно
- [ ] Rollback при ошибке
- [ ] Refetch после мутации
- [ ] WebSocket / polling для real-time updates?

#### E2. Локальное состояние
- [ ] React Query / SWR cache invalidation
- [ ] Stale data detection
- [ ] Background refetch

---

## 🔍 МЕТОДОЛОГИЯ АУДИТА

### Шаг 1: Статический анализ (2 часа)
1. Прочитать весь код в `frontend/src/`
2. Построить dependency graph
3. Найти все API calls к `/api/v1/ai/`
4. Проверить TypeScript типы vs реальные API responses

**Инструменты:**
```bash
# Найти все вызовы AI API
grep -r "ai/recognize" frontend/src/
grep -r "ai/task" frontend/src/

# Найти обработку ошибок
grep -r "еда не найдена" frontend/src/
grep -r "No food" frontend/src/
grep -r "not found" frontend/src/ -i
```

### Шаг 2: Динамический анализ (3 часа)
1. Запустить dev server
2. Открыть Browser DevTools
3. Протестировать flow:
   - Загрузить фото
   - Наблюдать Network tab
   - Проверить Console на ошибки
   - Проверить React DevTools state

**Тест-кейсы:**
```
TC-1: Успешное распознавание
  1. Загрузить фото с едой
  2. Ожидать: Loader показывается
  3. Ожидать: Polling /api/v1/ai/task/<id>/ каждые 2-3 сек
  4. Ожидать: Результат отображается с продуктами

TC-2: AI не распознал еду
  1. Загрузить пустое/чёрное фото
  2. Ожидать: Сообщение "Не удалось распознать продукты на фото"
  3. НЕ ожидать: "Еда не найдена" (неправильное сообщение)

TC-3: Network timeout
  1. Включить throttling в DevTools
  2. Загрузить фото
  3. Ожидать: Retry или понятное сообщение об ошибке

TC-4: Быстрая повторная загрузка
  1. Загрузить фото 1
  2. Сразу загрузить фото 2
  3. Ожидать: Первый запрос отменён, показывается только результат фото 2
```

### Шаг 3: Code review (2 часа)
Проверить антипаттерны:

**React антипаттерны:**
- [ ] `useEffect` с пустым dependency array для fetch
- [ ] Прямая мутация state
- [ ] Отсутствие cleanup в useEffect
- [ ] Избыточные re-renders

**TypeScript антипаттерны:**
- [ ] `any` вместо proper types
- [ ] Type assertions без проверок
- [ ] Optional chaining `?.` скрывает реальные проблемы

**Async антипаттерны:**
- [ ] Unhandled promise rejections
- [ ] Race conditions
- [ ] Memory leaks в pending requests

---

## 📊 DELIVERABLES (Результаты аудита)

### 1. Отчёт об аудите
**Файл:** `frontend/AUDIT_REPORT.md`

**Структура:**
```markdown
# Отчёт об аудите фронтенда EatFit24

## Executive Summary
- Всего найдено багов: X
- Критических: X
- Высокий приоритет: X
- Средний приоритет: X

## Критические баги
### BUG-001: "Еда не найдена" при успешном распознавании
**Severity:** CRITICAL
**Component:** AI Recognition Flow
**Root Cause:** [описание причины]
**Impact:** Пользователи не могут добавлять еду
**Fix:** [описание решения]
**Files affected:** [...список файлов...]

## Список всех багов
[Таблица со всеми багами]

## Рекомендации
[Архитектурные улучшения]
```

### 2. Bug tracker spreadsheet
**Файл:** `frontend/BUGS_TRACKER.csv`

| ID | Severity | Component | Description | Root Cause | Status | Assignee | Files |
|----|----------|-----------|-------------|------------|--------|----------|-------|
| BUG-001 | CRITICAL | AI Recognition | "Еда не найдена" на успешном recognition | [причина] | Open | Droid | [...] |
| BUG-002 | HIGH | Navigation | ... | ... | Open | ... | ... |

### 3. Roadmap устранения багов
**Файл:** `frontend/BUGFIX_ROADMAP.md`

---

## 🗺️ ROADMAP УСТРАНЕНИЯ БАГОВ

### Phase 1: HOTFIX критических багов (1 день)
**Цель:** Восстановить базовую функциональность

#### HOTFIX-1: Исправить "Еда не найдена" баг
**Приоритет:** P0 (BLOCKER)
**Время:** 4 часа

**План:**
1. [ ] Найти где генерируется сообщение "Еда не найдена"
2. [ ] Проверить условие `if (recognized_items.length === 0)`
3. [ ] Исправить парсинг API response
4. [ ] Добавить правильную обработку успешного результата
5. [ ] Тестирование на реальных данных

**Expected changes:**
```typescript
// BEFORE (предположительно)
if (!response.food_items || response.food_items.length === 0) {
  showError("Еда не найдена");
}

// AFTER
if (response.success && response.recognized_items?.length > 0) {
  // Display recognized items
  setRecognizedItems(response.recognized_items);
} else if (response.success && response.recognized_items?.length === 0) {
  showError("Не удалось распознать продукты на фото. Попробуйте другое фото.");
} else {
  showError(response.error || "Ошибка распознавания");
}
```

#### HOTFIX-2: Реализовать task polling (если отсутствует)
**Приоритет:** P0 (BLOCKER)
**Время:** 6 часов

**План:**
1. [ ] Создать `useTaskPolling` hook
2. [ ] Реализовать exponential backoff
3. [ ] Добавить timeout 60 секунд
4. [ ] Обработать все состояния: pending, success, failed
5. [ ] Интеграция в AI recognition flow

**Code template:**
```typescript
// frontend/src/hooks/useTaskPolling.ts
export function useTaskPolling(taskId: string | null) {
  const [status, setStatus] = useState<'idle' | 'polling' | 'success' | 'failed'>('idle');
  const [result, setResult] = useState(null);
  const [error, setError] = useState(null);

  useEffect(() => {
    if (!taskId) return;

    const startTime = Date.now();
    const MAX_DURATION = 60000; // 60 seconds

    const poll = async () => {
      try {
        const response = await api.get(`/api/v1/ai/task/${taskId}/`);

        if (response.data.success) {
          setStatus('success');
          setResult(response.data);
          return; // Stop polling
        }

        if (Date.now() - startTime > MAX_DURATION) {
          setStatus('failed');
          setError('Timeout: распознавание заняло слишком много времени');
          return;
        }

        // Continue polling
        setTimeout(poll, 3000); // Poll every 3 seconds
      } catch (err) {
        setStatus('failed');
        setError(err.message);
      }
    };

    setStatus('polling');
    poll();

    return () => {
      // Cleanup: cancel pending requests
    };
  }, [taskId]);

  return { status, result, error };
}
```

### Phase 2: Исправление багов HIGH priority (1 день)
**Цель:** Стабилизировать приложение

1. [ ] Исправить навигационные баги
2. [ ] Пофиксить timezone issues с датами
3. [ ] Добавить retry для failed network requests
4. [ ] Улучшить error messages (понятные пользователю)

### Phase 3: Улучшение UX и производительности (1 день)
**Цель:** Оптимизация пользовательского опыта

1. [ ] Оптимизировать loading states
2. [ ] Добавить skeleton screens
3. [ ] Устранить дублирование запросов
4. [ ] Мемоизация компонентов

### Phase 4: Code quality и рефакторинг (опционально)
**Цель:** Предотвратить будущие баги

1. [ ] Добавить TypeScript strict mode
2. [ ] Написать unit tests для критических flows
3. [ ] Добавить E2E тесты (Playwright/Cypress)
4. [ ] Настроить ESLint с strict rules

---

## 🎯 SUCCESS CRITERIA

### Must Have (критерии успеха)
- [x] Backend распознаёт продукты (УЖЕ РАБОТАЕТ)
- [ ] Фронтенд **отображает** распознанные продукты
- [ ] 0 ошибок "Еда не найдена" при успешном распознавании
- [ ] Polling работает с таймаутом 60 сек
- [ ] Понятные сообщения об ошибках для пользователя

### Should Have (желательно)
- [ ] Loading indicators во время распознавания
- [ ] Retry mechanism при network errors
- [ ] Optimistic UI updates
- [ ] Offline-first approach (cache results)

### Nice to Have (можно позже)
- [ ] WebSocket real-time updates вместо polling
- [ ] Progressive image upload
- [ ] Batch recognition (несколько фото)

---

## 📁 ФАЙЛЫ ДЛЯ ОСОБОГО ВНИМАНИЯ

### Критические файлы (скорее всего содержат баг)
```
frontend/src/
  ├── services/
  │   ├── aiService.ts          # API calls к AI endpoints
  │   └── api.ts                # Base API client
  ├── hooks/
  │   ├── useAIRecognition.ts   # AI recognition logic
  │   └── useTaskPolling.ts     # (может отсутствовать - создать!)
  ├── pages/
  │   └── FoodLogPage/
  │       ├── index.tsx
  │       └── PhotoUpload.tsx   # Компонент загрузки фото
  ├── components/
  │   ├── AIRecognitionResult.tsx
  │   └── ErrorMessage.tsx
  └── types/
      └── ai.ts                 # TypeScript interfaces
```

### Конфигурационные файлы
```
frontend/
  ├── package.json              # Зависимости
  ├── vite.config.ts           # Build config
  ├── tsconfig.json            # TypeScript config
  └── .env                     # Environment variables
```

---

## 🔧 ИНСТРУМЕНТЫ ДЛЯ АУДИТА

### Developer Tools
- **Browser DevTools:** Network tab, Console, React DevTools
- **VS Code Extensions:** ESLint, TypeScript, Error Lens
- **Network throttling:** Simulate slow 3G

### CLI Tools
```bash
# Найти все TODO/FIXME в коде
grep -r "TODO\|FIXME\|XXX\|HACK" frontend/src/

# Найти использование 'any' в TypeScript
grep -r ": any" frontend/src/

# Найти console.log (должны быть удалены в production)
grep -r "console\\.log" frontend/src/

# Проверить bundle size
npm run build
du -sh frontend/dist/assets/*.js

# TypeScript type check
cd frontend && npm run type-check

# Linting
cd frontend && npm run lint
```

### Automated testing
```bash
# Unit tests
npm run test

# E2E tests (если есть)
npm run test:e2e

# Coverage
npm run test:coverage
```

---

## 📞 КОММУНИКАЦИЯ

### Ежедневные updates
Создавать daily reports в формате:

**Файл:** `frontend/audit_logs/2025-12-06.md`
```markdown
# Daily Audit Report: 2025-12-06

## Progress
- Проверено компонентов: 15/40
- Найдено багов: 8
- Исправлено багов: 2

## Findings Today
1. BUG-001: Критический баг в AI recognition
2. BUG-008: Race condition в meal update

## Tomorrow's Plan
- Исправить BUG-001
- Протестировать fix на staging
```

### Final Presentation
После завершения аудита создать:
1. **Executive Summary** (1 страница)
2. **Technical Deep Dive** (полный отчёт)
3. **Demo video** (screencast с воспроизведением багов)

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Не ломать работающее!
- Перед изменениями делать backup
- Использовать feature flags для больших изменений
- Тестировать на staging перед production

### Приоритизация
Порядок важности:
1. **P0 (BLOCKER):** Критический функционал не работает
2. **P1 (HIGH):** Серьёзно влияет на UX
3. **P2 (MEDIUM):** Неудобства для пользователя
4. **P3 (LOW):** Косметические баги

### Security first
- НЕ коммитить sensitive data
- НЕ логировать токены/пароли
- Валидировать все пользовательские данные

---

## 📚 REFERENCE LINKS

### Backend API Documentation
- Async AI recognition: `POST /api/v1/ai/recognize/`
- Task status: `GET /api/v1/ai/task/<task_id>/`
- Swagger docs: `https://api.eatfit24.com/api/docs/`

### Current Backend Status (проверено логами)
```
✅ Redis: работает
✅ Celery Worker: работает
✅ AI Recognition: работает (3 успешных распознавания)
✅ Database: Meals + FoodItems создаются корректно
```

**Backend НЕ содержит багов. Проблема 100% на фронтенде.**

---

## ✅ ACCEPTANCE CRITERIA

Аудит считается завершённым когда:

1. [ ] Создан полный отчёт `AUDIT_REPORT.md`
2. [ ] Все критические баги (P0) задокументированы
3. [ ] Roadmap устранения багов готов
4. [ ] BUG-001 "Еда не найдена" исправлен и протестирован
5. [ ] Проведено end-to-end тестирование AI recognition flow
6. [ ] Нет regression bugs (старая функциональность не сломалась)

---

## 🚀 НАЧАТЬ АУДИТ

**Команда для старта:**
```bash
cd frontend
npm install
npm run dev

# В другом терминале
npm run type-check
npm run lint

# Начать проверку
code AUDIT_REPORT.md
```

**Первый шаг:** Найти где обрабатывается ответ от AI API
```bash
grep -r "recognize" frontend/src/ --include="*.ts" --include="*.tsx"
grep -r "еда не найдена" frontend/src/ -i
```

---

**Вопросы? Проблемы?**
Документировать всё в `frontend/audit_logs/` для трекинга прогресса.

Good luck, Droid! 🤖
