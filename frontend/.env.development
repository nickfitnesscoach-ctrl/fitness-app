# =============================================================================
# EatFit24 Frontend - Development Environment
# =============================================================================
# This file contains development-specific configuration.
# DO NOT COMMIT THIS FILE TO GIT (it's in .gitignore)
# Copy from .env.example and adjust values for your local setup.
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Base URL for backend API
# Uses relative path to enable Vite proxy (see vite.config.js)
# Vite will proxy /api/v1 requests to http://localhost:8000
VITE_API_URL=/api/v1

# -----------------------------------------------------------------------------
# Telegram Configuration
# -----------------------------------------------------------------------------
# Telegram bot name (production bot)
VITE_TELEGRAM_BOT_NAME=EatFit24_bot

# Trainer invite link (for invite client page)
VITE_TRAINER_INVITE_LINK=https://t.me/EatFit24_bot

# -----------------------------------------------------------------------------
# Development Flags
# -----------------------------------------------------------------------------
# Mock Telegram WebApp API (1 = enabled, 0 = disabled)
# Enable this to test the app in a regular browser without Telegram
VITE_MOCK_TELEGRAM=1

# Skip Telegram authentication (true/false)
# Enable this to bypass Telegram auth check during development
VITE_SKIP_TG_AUTH=true

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - All Vite env variables MUST start with VITE_ prefix
# - Use import.meta.env.VITE_* to access these variables in code
# - Built-in Vite variables (DEV, PROD, MODE) don't need VITE_ prefix
# - For production values, see .env.production
# - For template/documentation, see .env.example
