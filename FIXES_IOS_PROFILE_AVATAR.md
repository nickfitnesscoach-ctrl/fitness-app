# Исправление проблем с профилем и аватаром на iOS

## 📋 Краткое резюме

**Проблема:** На iOS не сохраняется профиль и не загружается аватар (ошибки "Failed to update profile" и "Failed to upload avatar")

**Причина:** Backend не поддерживал формат HEIC, который используется камерами iOS по умолчанию

**Решение:** Добавлена поддержка HEIC + улучшена обработка ошибок на backend и frontend

---

## 🔍 Найденные проблемы

### 1. ❌ КРИТИЧЕСКАЯ: Backend не поддерживал HEIC формат (iOS камеры)

**Файл:** `backend/apps/users/validators.py:178-179`

**Проблема:**
```python
AVATAR_ALLOWED_TYPES = ('image/jpeg', 'image/png', 'image/webp')
AVATAR_ALLOWED_EXTENSIONS = ('.jpg', '.jpeg', '.png', '.webp')
```

iOS камеры создают фотографии в формате HEIC, который НЕ был в списке разрешенных форматов.

**Результат:**
- ✅ Android: успешная загрузка (JPEG по умолчанию)
- ❌ iOS: ошибка "Failed to upload avatar" (HEIC не разрешен)

---

### 2. ⚠️ Frontend: Недостаточная обработка ошибок профиля

**Файл:** `frontend/src/components/ProfileEditModal.tsx:78`

**Проблема:**
- Показывался только `err.message` без деталей
- Не парсились field-level ошибки от backend
- Backend возвращал детальные ошибки валидации, но frontend их игнорировал

**Результат:**
- Пользователь видел общую ошибку вместо конкретной проблемы
- Например: "Ошибка при сохранении профиля" вместо "Рост должен быть от 50 до 250 см"

---

### 3. ⚠️ Frontend: Отсутствие валидации числовых полей

**Файл:** `frontend/src/components/ProfileEditModal.tsx:64-67`

**Проблема:**
```typescript
if (formData.height) {
    payload.height = Number(formData.height); // Может быть NaN!
}
```

Если пользователь ввел некорректное значение, `Number()` возвращал `NaN`, который отправлялся на backend.

**Результат:**
- Backend отклонял запрос с некорректными данными
- Пользователь видел непонятную ошибку

---

### 4. ⚠️ Frontend: Минимальное логирование загрузки аватара

**Файл:** `frontend/src/pages/ProfilePage.tsx:237-276`

**Проблема:**
- Не логировались тип файла и размер
- Не было специальной обработки HEIC формата
- Общие сообщения об ошибках без контекста

---

### 5. ⚠️ Backend: Недостаточное логирование ошибок

**Файл:** `backend/apps/users/views.py:231-266`

**Проблема:**
- Не логировалась платформа пользователя (iOS vs Android)
- Validation errors не логировались перед отправкой клиенту
- Трудно было отследить причину проблем на iOS

---

## ✅ Внедренные исправления

### Backend

#### 1. Добавлена поддержка HEIC/HEIF форматов

**Файл:** `backend/apps/users/validators.py`

```python
# Было:
AVATAR_ALLOWED_TYPES = ('image/jpeg', 'image/png', 'image/webp')
AVATAR_ALLOWED_EXTENSIONS = ('.jpg', '.jpeg', '.png', '.webp')

# Стало:
# iOS cameras produce HEIC by default - we support it
AVATAR_ALLOWED_TYPES = ('image/jpeg', 'image/png', 'image/webp', 'image/heic', 'image/heif')
AVATAR_ALLOWED_EXTENSIONS = ('.jpg', '.jpeg', '.png', '.webp', '.heic', '.heif')
```

**Результат:**
- ✅ iOS пользователи могут загружать фото с камеры
- ✅ Android пользователи продолжают работать как раньше
- ✅ Поддержка всех современных форматов изображений

---

#### 2. Улучшена обработка ошибок валидации аватара

**Файл:** `backend/apps/users/views.py`

**Добавлено:**
- Структурированные ошибки с кодом и описанием
- Логирование всех validation errors
- Понятные сообщения для пользователя

```python
except DjangoValidationError as e:
    error_message = str(e.message) if hasattr(e, 'message') else str(e)
    error_code = e.code if hasattr(e, 'code') else 'validation_error'

    logger.warning(
        f"Avatar validation failed for user {request.user.id}: {error_code} - {error_message}"
    )

    return Response({
        "error": error_message,
        "code": error_code,
        "detail": error_message
    }, status=400)
```

---

#### 3. Расширено логирование ProfileView

**Файл:** `backend/apps/users/views.py`

**Добавлено:**
- Логирование платформы (iOS/Android/Desktop)
- User-Agent для диагностики
- Детальное логирование ошибок с контекстом

```python
platform = request.META.get('HTTP_SEC_CH_UA_PLATFORM', 'unknown')
user_agent = request.META.get('HTTP_USER_AGENT', 'unknown')[:100]
logger.info(
    f"[ProfileView] {'PATCH' if partial else 'PUT'} - "
    f"User: {user.id}, Platform: {platform}, Data: {request.data}"
)
```

**Результат:**
- ✅ Теперь можно отследить проблемы специфичные для iOS
- ✅ Логи содержат всю необходимую информацию для отладки

---

### Frontend

#### 4. Добавлена валидация числовых полей перед отправкой

**Файл:** `frontend/src/components/ProfileEditModal.tsx`

```typescript
// Validate numeric fields before sending
if (formData.height) {
    const height = Number(formData.height);
    if (isNaN(height) || height < 50 || height > 250) {
        setError('Рост должен быть от 50 до 250 см');
        setIsSaving(false);
        return;
    }
    payload.height = height;
}

if (formData.weight) {
    const weight = Number(formData.weight);
    if (isNaN(weight) || weight < 20 || weight > 500) {
        setError('Вес должен быть от 20 до 500 кг');
        setIsSaving(false);
        return;
    }
    payload.weight = weight;
}
```

**Результат:**
- ✅ Некорректные данные отклоняются на frontend до отправки
- ✅ Понятные сообщения об ошибках для пользователя
- ✅ Меньше ненужных запросов к backend

---

#### 5. Улучшена обработка ошибок обновления профиля

**Файл:** `frontend/src/components/ProfileEditModal.tsx`

**Добавлено:**
- Парсинг field-level ошибок от backend
- Человекочитаемые названия полей на русском
- Детальные сообщения об ошибках

```typescript
// Parse field-level errors from backend
if (typeof errorData === 'object' && !errorData.detail && !errorData.error) {
    const fieldErrors = Object.entries(errorData)
        .map(([field, messages]) => {
            const fieldName = field === 'birth_date' ? 'Дата рождения' :
                            field === 'height' ? 'Рост' :
                            field === 'weight' ? 'Вес' :
                            field === 'gender' ? 'Пол' :
                            field;
            return `${fieldName}: ${Array.isArray(messages) ? messages.join(', ') : messages}`;
        })
        .join('\n');

    if (fieldErrors) {
        errorMessage = fieldErrors;
    }
}
```

**Результат:**
- ✅ Пользователь видит конкретную проблему: "Рост должен быть от 50 до 250 см"
- ✅ Не нужно гадать, что пошло не так

---

#### 6. Улучшено логирование и обработка HEIC при загрузке аватара

**Файл:** `frontend/src/pages/ProfilePage.tsx`

**Добавлено:**
- Детальное логирование файла (имя, тип, размер)
- Определение HEIC формата
- Понятные сообщения об ошибках
- Очистка file input после загрузки

```typescript
console.log('[ProfilePage] Selected file:', {
    name: file.name,
    type: file.type,
    size: file.size,
    sizeKB: (file.size / 1024).toFixed(1)
});

// iOS-specific: Check for HEIC format
if (file.type === 'image/heic' || file.name.toLowerCase().endsWith('.heic')) {
    console.log('[ProfilePage] HEIC image detected, uploading directly');
}

// Better error messages
if (errorMessage.includes('формат') || errorMessage.includes('format')) {
    errorMessage += ' На iOS фотографии могут быть в формате HEIC. Попробуйте сохранить фото как JPEG.';
}
```

**Результат:**
- ✅ Легко отследить проблемы через консоль
- ✅ Понятные подсказки для пользователя
- ✅ Можно повторно выбрать тот же файл

---

#### 7. Добавлено логирование успешных операций

**Файл:** `frontend/src/components/ProfileEditModal.tsx`, `frontend/src/pages/ProfilePage.tsx`

```typescript
console.log('[ProfileEditModal] Updating profile with payload:', payload);
console.log('[ProfileEditModal] Profile updated successfully:', updated);

console.log('[ProfilePage] Avatar uploaded successfully:', updatedProfile.avatar_url);
```

**Результат:**
- ✅ Можно отследить успешные операции для отладки
- ✅ Легче воспроизвести проблемы

---

## 📊 Что работало правильно (без изменений)

1. ✅ **Backend аутентификация через TelegramHeaderAuthentication** - корректна для iOS WebView
2. ✅ **PATCH метод для частичного обновления профиля** - поддерживается
3. ✅ **Avatar versioning для cache busting** - работает
4. ✅ **Валидация размера файла (5 МБ)** - корректна
5. ✅ **Serializers для Profile** - правильные типы полей

---

## 🧪 Тестирование

### Сценарии для проверки на iOS:

1. **Загрузка аватара с камеры:**
   - Открыть профиль → нажать на аватар → сделать фото → сохранить
   - **Ожидаемый результат:** Фото успешно загружается (HEIC поддерживается)

2. **Загрузка аватара из галереи:**
   - Открыть профиль → нажать на аватар → выбрать фото из галереи
   - **Ожидаемый результат:** Фото успешно загружается

3. **Обновление профиля с корректными данными:**
   - Редактировать профиль → изменить рост/вес → сохранить
   - **Ожидаемый результат:** Профиль сохраняется, данные обновляются

4. **Обновление профиля с некорректными данными:**
   - Редактировать профиль → ввести рост 999 → сохранить
   - **Ожидаемый результат:** Ошибка "Рост должен быть от 50 до 250 см"

5. **Загрузка слишком большого файла:**
   - Выбрать фото >5 МБ
   - **Ожидаемый результат:** Ошибка "Размер файла X МБ превышает лимит 5 МБ"

### Сценарии для проверки на Android и Desktop:

- Повторить все те же сценарии
- **Ожидаемый результат:** Всё продолжает работать как раньше (регрессий нет)

---

## 📁 Измененные файлы

### Backend:
1. ✅ `backend/apps/users/validators.py` - добавлена поддержка HEIC, улучшены сообщения об ошибках
2. ✅ `backend/apps/users/views.py` - улучшено логирование и обработка ошибок

### Frontend:
3. ✅ `frontend/src/components/ProfileEditModal.tsx` - валидация числовых полей, парсинг ошибок
4. ✅ `frontend/src/pages/ProfilePage.tsx` - улучшенное логирование, обработка HEIC

---

## 🚀 Следующие шаги

1. **Протестировать на реальном iOS устройстве:**
   - Загрузка фото с камеры (HEIC)
   - Загрузка фото из галереи
   - Редактирование профиля

2. **Протестировать на Android и Desktop:**
   - Убедиться, что нет регрессий

3. **Проверить логи backend:**
   - Убедиться, что платформа и ошибки логируются корректно

4. **Опционально: Конвертация HEIC → JPEG на backend**
   - Если HEIC файлы создают проблемы с хранением/отображением
   - Можно добавить автоматическую конвертацию с помощью Pillow + pillow-heif

---

## 📝 Выводы

### Главная проблема:
Backend не поддерживал формат HEIC, который используется iOS камерами по умолчанию.

### Решение:
- ✅ Добавлена поддержка HEIC/HEIF форматов в валидаторы
- ✅ Улучшена обработка ошибок и логирование на backend и frontend
- ✅ Добавлена валидация на frontend перед отправкой данных
- ✅ Понятные сообщения об ошибках для пользователя

### Результат:
Теперь пользователи iOS могут:
- ✅ Загружать аватары с камеры (HEIC)
- ✅ Редактировать профиль
- ✅ Получать понятные сообщения об ошибках

При этом функциональность на Android и Desktop не изменилась (нет регрессий).
