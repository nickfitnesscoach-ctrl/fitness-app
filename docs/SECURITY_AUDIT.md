# EatFit24 Security Audit Report

**Дата:** 2025-12-26
**Аудитор:** DevOps Agent
**Scope:** Production .env configuration и доступы

---

## 🔴 КРИТИЧЕСКИЕ уязвимости

### 1. Django SECRET_KEY не настроен

**Расположение:** `.env` строка ~20
```bash
SECRET_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_-hex_32
```

**Проблема:**
- Используется placeholder текст вместо реального ключа
- Django использует SECRET_KEY для криптографических операций (сессии, CSRF, подписи)
- Компрометация ключа = компрометация всех сессий и подписанных данных

**Есть дубликат:**
```bash
DJANGO_SECRET_KEY=4rmqovlZ7tD8jTuiY2SfAh5ppUEfbKxv/n+zfx9/k0j82aMAFfo7T+JIpdFhDmpD
```

**Риск:** 🔴 КРИТИЧЕСКИЙ
**Вероятность эксплуатации:** Высокая (если Django использует переменную `SECRET_KEY`)

**Решение:**
```bash
# Удалить старую строку SECRET_KEY
# Переименовать DJANGO_SECRET_KEY в SECRET_KEY
SECRET_KEY=4rmqovlZ7tD8jTuiY2SfAh5ppUEfbKxv/n+zfx9/k0j82aMAFfo7T+JIpdFhDmpD
```

---

### 2. Swagger API документация с дефолтным паролем

**Расположение:** `.env`
```bash
SWAGGER_AUTH_USERNAME=admin
SWAGGER_AUTH_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_-base64_16
```

**Проблема:**
- API документация защищена дефолтным паролем
- Публично доступна схема всех endpoints
- Возможна разведка перед атакой

**Риск:** 🟠 ВЫСОКИЙ
**Вероятность эксплуатации:** Средняя (если /swagger/ не закрыт в Nginx)

**Решение:**
```bash
# Сгенерировать новый пароль
SWAGGER_AUTH_PASSWORD=$(openssl rand -base64 16)

# Или отключить Swagger в production полностью (рекомендуется)
# В config/settings/production.py:
# SPECTACULAR_SETTINGS = {'SERVE_INCLUDE_SCHEMA': False}
```

---

### 3. Django Admin недоступен (нет суперюзеров)

**Проблема:**
- В базе данных **0 суперюзеров**
- Невозможно войти в админку `/admin/`
- Нет контроля над пользователями и данными через UI

**Риск:** 🟡 СРЕДНИЙ (не уязвимость, а проблема доступа)

**Решение:**
```bash
# Создать суперюзера
ssh deploy@eatfit24.ru
cd /opt/EatFit24
docker compose exec backend python manage.py createsuperuser
```

**Рекомендуемые данные:**
- Username: `admin`
- Email: `admin@eatfit24.ru`
- Password: `<генерировать сильный пароль>`

---

## 🟠 Высокий приоритет

### 4. AI API ключ не настроен

**Расположение:** `.env`
```bash
OPENROUTER_API_KEY=CHANGE_ME_GET_FROM_OPENROUTER
```

**Проблема:**
- AI функции не работают
- Сервис заявлен как "AI-powered", но ИИ не активен

**Риск:** 🟡 СРЕДНИЙ (функциональность, не безопасность)

**Решение:**
1. Получить ключ на https://openrouter.ai
2. Обновить `.env`:
   ```bash
   OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

---

### 5. YooKassa в тестовом режиме

**Расположение:** `.env`
```bash
YOOKASSA_MODE=test
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=test_your_secret_key_or_live_your_secret_key
```

**Проблема:**
- Реальные платежи не обрабатываются
- Пользователи не могут купить PRO подписку
- Сервис не монетизируется

**Риск:** 🟡 СРЕДНИЙ (бизнес, не безопасность)

**Решение:**
1. Получить production ключи в YooKassa
2. Обновить `.env`:
   ```bash
   YOOKASSA_MODE=prod
   YOOKASSA_SHOP_ID=<реальный shop_id>
   YOOKASSA_SECRET_KEY=live_xxxxxxxxxxxxxxxxxx
   ```

---

### 6. Токен Telegram бота в открытом виде

**Расположение:** `.env`
```bash
TELEGRAM_BOT_TOKEN=7611657073:AAG4oviiDPJH-oN3EIIEqvBzg1J28XhCBpc
```

**Проблема:**
- Токен бота находится в .env (нормально)
- НО если .env попадёт в репозиторий = компрометация бота

**Риск:** 🟡 СРЕДНИЙ (зависит от контроля .gitignore)

**Текущий статус:**
- ✅ `.env` в `.gitignore`
- ✅ Токен не в репозитории
- ⚠️ Токен в production доступен через SSH

**Рекомендации:**
- Регулярно ротировать токен через @BotFather
- Использовать secrets manager (AWS Secrets Manager, HashiCorp Vault) для критичных токенов

---

## 🟢 Низкий приоритет (улучшения)

### 7. Дублирование переменных

```bash
SECRET_KEY=CHANGE_ME...
DJANGO_SECRET_KEY=4rmqovlZ...  # Дубликат!

ALLOWED_HOSTS=eatfit24.ru,...
DJANGO_ALLOWED_HOSTS=eatfit24.ru,...  # Дубликат!
```

**Проблема:**
- Непонятно какая переменная используется
- Риск конфликтов

**Решение:**
- Оставить только одну версию каждой переменной
- Проверить в `config/settings/production.py` какая используется

---

### 8. Пароль БД в .env

```bash
POSTGRES_PASSWORD=c439d0524d76a12a98f2c25fa4d47d70b06beb476f653f1b
```

**Статус:** ✅ Хороший длинный пароль (48 символов hex)

**Рекомендация:**
- Менять при каждом инциденте безопасности
- Использовать secrets manager в будущем

---

## 📊 Оценка общей безопасности

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| Секреты Django | 🟠 5/10 | SECRET_KEY placeholder, есть дубликат DJANGO_SECRET_KEY |
| API доступы | 🟠 6/10 | Swagger с дефолтным паролем |
| База данных | ✅ 9/10 | Сильный пароль, не exposed наружу |
| Токены | ✅ 8/10 | Хранятся правильно, но не ротируются |
| Платежи | 🟡 7/10 | В тестовом режиме (не проблема безопасности) |
| Общая оценка | 🟠 **7/10** | Средняя безопасность, требуются исправления |

---

## ✅ Что сделано правильно

1. ✅ `.env` в `.gitignore` - не попадёт в репозиторий
2. ✅ `DEBUG=False` - production режим
3. ✅ `ALLOWED_HOSTS` ограничен доменами
4. ✅ `POSTGRES_PASSWORD` - сильный пароль
5. ✅ `WEBHOOK_TRUST_XFF=true` + `WEBHOOK_TRUSTED_PROXIES` настроены для Nginx
6. ✅ `SECURE_SSL_REDIRECT=False` правильно (Nginx терминирует SSL)
7. ✅ Database и Redis не exposed наружу (только Docker network)

---

## 🛠️ Action Plan

### Критичные (сделать немедленно)

- [ ] Исправить `SECRET_KEY` - удалить placeholder, использовать `DJANGO_SECRET_KEY`
- [ ] Сгенерировать новый `SWAGGER_AUTH_PASSWORD` или отключить Swagger
- [ ] Создать Django суперюзера для доступа в админку

### Высокий приоритет (эта неделя)

- [ ] Получить `OPENROUTER_API_KEY` и активировать AI функции
- [ ] Настроить production YooKassa (если планируется монетизация)
- [ ] Удалить дублирующиеся переменные (SECRET_KEY vs DJANGO_SECRET_KEY)

### Средний приоритет (этот месяц)

- [ ] Настроить ротацию Telegram токена (раз в 3-6 месяцев)
- [ ] Рассмотреть использование secrets manager
- [ ] Добавить мониторинг попыток несанкционированного доступа

### Низкий приоритет (квартал)

- [ ] Внедрить 2FA для Django admin
- [ ] Настроить rate limiting для API endpoints
- [ ] Провести penetration testing

---

## 📝 Checklist для проверки .env перед деплоем

```bash
# 1. Проверить что НЕТ placeholder значений
grep -E "CHANGE_ME|your_|test_your" .env

# 2. Проверить что SECRET_KEY настроен
grep "^SECRET_KEY=" .env

# 3. Проверить что DEBUG=False
grep "^DEBUG=" .env

# 4. Проверить ALLOWED_HOSTS
grep "^ALLOWED_HOSTS=" .env

# 5. Проверить что .env в .gitignore
git check-ignore .env
```

**Expected output:** Только пустой вывод или "False" для DEBUG

---

## 🔗 Ссылки

- [Django Security Checklist](https://docs.djangoproject.com/en/stable/howto/deployment/checklist/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Python Security Best Practices](https://python.readthedocs.io/en/stable/library/secrets.html)

---

**End of Report**
