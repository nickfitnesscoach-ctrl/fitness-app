# UI Rule: Разделение ответственности экранов

## Принцип

**Каждый экран решает одну задачу. Не смешивать процесс с анализом результата.**

---

## 📸 Экран "Камера" (процесс)

**Задача:** Загрузить фото → Дождаться распознавания → Завершить

**Что показываем:**
- ✅ Статус фото (успех / ошибка / отменено)
- ✅ Итоги загрузки (N из M распознано)
- ✅ Кнопки процесса:
  - "Повторить выбранные"
  - "Готово"
  - "Вернуться в камеру"

**Что НЕ показываем:**
- ❌ Кнопка "Детали"
- ❌ Редактирование блюд
- ❌ Переход в анализ КБЖУ
- ❌ Любые действия с результатом

**Почему:**
- Пользователь ещё в процессе загрузки
- Провал в детали создаёт потерю контекста
- Появляются дубли сценариев (камера ≈ дневник)
- Рассинхронизация состояния

---

## 📔 Экран "Дневник" (результат)

**Задача:** Работа с приёмами пищи

**Что показываем:**
- ✅ Карточки приёмов пищи
- ✅ Кнопка "Открыть детали"
- ✅ Работа с блюдами (редактирование, удаление)
- ✅ Анализ КБЖУ

**Логика:**
- Камера = снял
- Дневник = разобрал

---

## 🔍 Экран "Детали приёма пищи" (анализ)

**Вход:** Только из дневника (не из камеры!)

**Что показываем:**
- ✅ Фотографии приёма пищи
- ✅ Список распознанных блюд
- ✅ КБЖУ каждого блюда
- ✅ Кнопки редактирования
- ✅ Кнопка удаления приёма

---

## Правило

> **Ни один экран не должен давать доступ к "Деталям", пока пользователь не оказался в дневнике.**

### Преимущества:
- Упрощает UX
- Снижает количество багов
- Делает путь пользователя линейным и понятным
- Нет дублирования функционала

### Единственное исключение (не для MVP):
Быстрый **read-only** предпросмотр (без кнопок), если будет нужен по метрикам.

Но для текущей версии — нет. Это лишнее.

---

## Чек-лист проверки экранов

При добавлении новой функции спросить:

1. **Это процесс или результат?**
   - Процесс → Камера
   - Результат → Дневник

2. **Нужен ли доступ к деталям?**
   - Если да → Только из дневника
   - Если нет → Убрать

3. **Не дублируется ли функционал?**
   - Камера и Дневник не должны пересекаться

---

## Статус: ✅ Активно

Дата введения: 2025-12-29
