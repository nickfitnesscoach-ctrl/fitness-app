# Руководство по разработке Telegram Mini App (Dev Environment)

Этот документ описывает, как поднять локальное окружение для разработки Telegram Mini App, используя туннелирование и режим отладки.

## 🏗 Архитектура

В отличие от обычной веб-разработки, Telegram Mini App требует **публичного HTTPS URL**, так как Telegram не может обращаться к `localhost`.

**Схема потока данных:**
```
Telegram App (Телефон/Десктоп) 
      ⬇️
🌍 Cloudflare Tunnel (https://....trycloudflare.com)
      ⬇️
🖥️ Ваш компьютер (localhost:3000)
      ⬇️
📦 Docker Container: Nginx (frontend)
      ⬇️
📦 Docker Container: Backend (Django)
```

---

## 🚀 Быстрый Старт

### 1. Запуск Туннеля
Откройте отдельный терминал и выполните команду для создания публичного адреса:

```powershell
cloudflared tunnel --url http://localhost:3000 --protocol http2
```

🛑 **Не закрывайте этот терминал!**
Скопируйте полученную ссылку, например: `https://tba-codes.trycloudflare.com`.

### 2. Настройка `env.local`
Убедитесь, что в вашем файле `.env.local` (в корне проекта) прописаны настройки для туннеля:

```ini
# Ссылка из шага 1
WEB_APP_URL=https://tba-codes.trycloudflare.com/app
TRAINER_PANEL_URL=https://tba-codes.trycloudflare.com/panel

# Разрешаем Django принимать запросы с этого домена
# ВАЖНО: Добавьте .trycloudflare.com в список!
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend,db,redis,.trycloudflare.com
```

### 3. Запуск Проекта (Docker)
Скопируйте шаблон и запустите окружение:

```powershell
# 1. Скопируйте шаблон (первый раз или после изменений .env.local)
cp .env.local .env

# 2. Запустите контейнеры
docker compose -f compose.yml -f compose.dev.yml up -d --build
```

> ⚠️ **SSOT**: Docker Compose всегда читает `.env`. Флаг `--env-file` **запрещён**.

---

## 🐞 Режим Отладки (Debug Mode)

В локальной разработке у нас нет настоящей подписи `initData` от Telegram (так как мы открываем приложение в браузере или эмуляторе без полного контекста).

### Как это работает?
1. **Frontend**: Если видит параметр `?debug=1` в URL, он подставляет фейковые данные пользователя (ID 100) и отправляет специальный заголовок `X-Debug-Mode: true`.
2. **Backend**: Если видит заголовок `X-Debug-Mode: true` И включен `DEBUG=True` в настройках, он пропускает проверку подписи Telegram и авторизует вас как тестового пользователя.

### Как пользоваться?
Просто откройте ваш туннель в браузере с параметром `debug=1`:

🔗 `https://tba-codes.trycloudflare.com/app?debug=1`

Вы должны увидеть интерфейс приложения и автоматически авторизоваться ("Привет, [Имя Тестового Юзера]!").

---

## 🛠 Устранение неполадок (Troubleshooting)

### 1. Ошибка 502 Bad Gateway
* **Причина**: Фронтенд (Nginx) не может достучаться до Бэкенда, или Бэкенд еще запускается.
* **Решение**: Проверьте логи бэкенда `docker logs eatfit24_dev-backend-1`. Подождите пару минут.

### 2. Ошибка "DisallowedHost" (в логах бэкенда)
* **Причина**: Django отклонил запрос, так как домен туннеля не в списке `ALLOWED_HOSTS`.
* **Решение**: 
    1. Проверьте `.env`, чтобы там был `.trycloudflare.com`.
    2. Пересоздайте контейнер бэкенда: 
       ```powershell
       docker compose -f compose.yml -f compose.dev.yml up -d --force-recreate backend
       ```

### 3. Белый экран или вечная загрузка
* **Причина**: Ошибка JS или неправильный API URL.
* **Решение**: Откройте консоль разработчика в браузере (F12) и посмотрите вкладку Console и Network. Убедитесь, что запросы идут на `/api/v1/...` и получают ответ 200.

### 4. Ошибка AI: "Сервер временно недоступен" (AI_SERVER_ERROR)
* **Причина**: Docker контейнер не может найти адрес AI Proxy по доменному имени (проблемы DNS/VPN).
* **Решение**:
  1. В `.env.local` используйте **прямой IP-адрес** прокси, а не домен.
     ```ini
     AI_PROXY_URL=http://185.171.80.128:8001
     ```
  2. Убедитесь, что ваш компьютер (хост) имеет доступ к этому IP (включите VPN, если нужно).

### 5. Картинки не грузятся (404 Not Found)
* **Причина**: Nginx ошибочно пытается отдать картинки как статику.
* **Решение**:
  1. В `frontend/nginx.conf` должен быть блок с приоритетом:
     ```nginx
     location ^~ /media/ { ... }
     ```
  2. После правки конфига обязательно пересоберите фронт:
     ```powershell
     docker compose -f compose.yml -f compose.dev.yml up -d --build frontend
     ```

---

## 📋 Чек-лист перед началом работы
1. [ ] Туннель запущен (`cloudflared`).
2. [ ] Ссылка туннеля обновлена в `.env.local` (`WEB_APP_URL`).
3. [ ] Домен туннеля есть в `ALLOWED_HOSTS`.
4. [ ] Docker контейнеры запущены (статус `healthy`).
5. [ ] Приложение открывается с `?debug=1`.
