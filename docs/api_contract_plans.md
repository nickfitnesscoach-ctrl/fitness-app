# API Contract: AI Personal Plan

SSOT — единственный источник истины

Этот документ описывает, как бот и backend обязаны взаимодействовать при создании опроса и сохранении персонального AI-плана.
Любое отклонение от этого контракта считается багом.

Общие принципы

План нельзя показывать пользователю, пока он не сохранён в БД

Каждый запрос обязан быть трассируемым

Повторные запросы не должны создавать дубликаты

Ошибки должны быть понятны пользователю и полезны разработчику

Обязательные HTTP-заголовки
Заголовок	Назначение	Правила
X-Request-ID	ID запроса для трассировки	Обязателен. Генерируется ботом. Backend обязан вернуть тот же ID в ответе
X-Bot-Secret	Аутентификация бота	Обязателен для всех запросов

❗ Если X-Request-ID отсутствует — это ошибка реализации бота.

Эндпоинты
1️⃣ Отправка опроса

POST /api/telegram/personal-plan/survey/

Используется для сохранения ответов пользователя на опрос.

Поведение

Backend сохраняет данные опроса

Возвращает survey_id, который далее используется для создания плана

Ответы
Статус	Значение
201 Created	Опрос успешно сохранён
Примечания

Желательно (но не критично) обеспечить идемпотентность

Этот эндпоинт не генерирует план

2️⃣ Сохранение персонального плана

POST /api/telegram/personal-plan/plan/

Используется только после генерации текста плана ботом.

Ключевое правило

План считается созданным только после успешного ответа backend.
До этого момента бот НЕ ИМЕЕТ ПРАВА показывать текст пользователю.

Успешные ответы
Статус	Когда возвращается
201 Created	План создан впервые
200 OK	План уже существует (идемпотентность)

⚠️ Повторный запрос не создаёт новый план, а возвращает уже существующий.

Ошибки (строго определённые)
HTTP	error.code	Значение	Поведение бота
429	DAILY_LIMIT_REACHED	Превышен лимит (макс. 3 плана в сутки)	❌ Без retry, показать сообщение
500	PLAN_SAVE_FAILED	Ошибка сохранения в БД	🔁 Разрешён retry
404	SURVEY_NOT_FOUND	Опрос не найден или не принадлежит пользователю	❌ Без retry
Формат ошибки (обязателен)
{
  "status": "error",
  "error": {
    "code": "PLAN_SAVE_FAILED",
    "message": "Не удалось сохранить план",
    "details": "Internal database error"
  }
}

Наблюдаемость и логирование (обязательно)
Backend обязан логировать:
<RID> | user_id | survey_id | plan_id | status


Пример:

f9a1c2... | user=12345 | survey=77 | plan=42 | created

Бот обязан логировать:
<RID> | http_status | error.code


Пример:

f9a1c2... | 500 | PLAN_SAVE_FAILED


Это позволяет за 10 секунд найти проблему от кнопки в Telegram до SQL-транзакции.

Retry-политика (строго)
🔁 Разрешён повтор (retry)

Таймауты

Сетевые ошибки

502 / 503 / 504

⛔ Запрещён повтор

429 DAILY_LIMIT_REACHED

400 VALIDATION_ERROR

403 FORBIDDEN

404 NOT_FOUND

Бот не имеет права повторять запросы при бизнес-ошибках.

Гарантии контракта

Если контракт соблюдён:

❌ Пользователь никогда не увидит «план, который не сохранился»

❌ В БД никогда не будет дубликатов

✅ Любой инцидент можно воспроизвести по X-Request-ID

✅ Поведение системы предсказуемо и безопасно

Статус документа

SSOT: ✅

Production-ready: ✅

Обязателен для бота и backend: ✅