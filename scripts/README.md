# Операционные скрипты EatFit24

Коллекция утилит для управления production инфраструктурой.

## Доступные скрипты

### 🔄 reset-celery-beat.sh

**Назначение:** Пересоздаёт Celery Beat контейнер для синхронизации с кодом

**Когда использовать:**
- После изменения `app.conf.beat_schedule` в коде
- Если периодические задачи не выполняются
- Если расписание не соответствует коду

**Использование:**
```bash
cd /opt/EatFit24
./scripts/reset-celery-beat.sh
```

**Что делает:**
1. Показывает текущее расписание из кода
2. Останавливает celery-beat
3. Удаляет контейнер (файл `celerybeat-schedule` теряется)
4. Создаёт новый контейнер (файл генерируется из кода)
5. Проверяет успешный запуск

**Документация:** [docs/operations/celery-beat.md](../docs/operations/celery-beat.md)

---

### 🧪 smoke_test.sh

**Назначение:** Быстрая проверка работоспособности production окружения

**Использование:**
```bash
cd /opt/EatFit24
sudo ./scripts/smoke_test.sh
```

---

## Требования

- Все скрипты должны запускаться из `/opt/EatFit24`
- Требуются права sudo для операций с Docker
- Рекомендуется проверять вывод скриптов для диагностики

## Добавление новых скриптов

При создании нового скрипта:

1. Добавьте shebang и set -e:
   ```bash
   #!/bin/bash
   set -e  # Exit on error
   ```

2. Добавьте описание в комментариях
3. Используйте цвета для вывода (см. reset-celery-beat.sh)
4. Обновите этот README
5. Сделайте скрипт исполняемым: `chmod +x script.sh`

## История

| Дата       | Скрипт               | Автор        |
|------------|----------------------|--------------|
| 2025-12-24 | reset-celery-beat.sh | DevOps Agent |
