# EatFit24 AI Proxy - Полное описание проекта

## Оглавление

1. [Обзор проекта](#1-обзор-проекта)
2. [Архитектура и технологический стек](#2-архитектура-и-технологический-стек)
3. [Структура проекта](#3-структура-проекта)
4. [Основные компоненты системы](#4-основные-компоненты-системы)
5. [API документация](#5-api-документация)
6. [Безопасность и аутентификация](#6-безопасность-и-аутентификация)
7. [Развертывание и DevOps](#7-развертывание-и-devops)
8. [Настройка и конфигурация](#8-настройка-и-конфигурация)
9. [Процесс разработки](#9-процесс-разработки)
10. [Дорожная карта и будущее развитие](#10-дорожная-карта-и-будущее-развитие)

---

## 1. Обзор проекта

### 1.1 Назначение

**EatFit24 AI Proxy** — это микросервис-прокси, который обеспечивает интеграцию между основным приложением EatFit24 и AI-моделями через OpenRouter API. Сервис предназначен для распознавания еды на фотографиях и автоматического расчета калорийности и макронутриентов (КБЖУ).

### 1.2 Ключевые функции

- **Распознавание продуктов питания** на загруженных изображениях
- **Автоматический расчет КБЖУ** (калории, белки, жиры, углеводы)
- **Оценка веса порций** в граммах
- **Поддержка мультиязычности** (русский и английский)
- **Учет комментариев пользователя** для уточнения распознавания
- **Безопасная работа** через VPN и API-ключи

### 1.3 Бизнес-ценность

Сервис решает критическую задачу для фитнес-приложения EatFit24:
- Автоматизирует процесс подсчета калорий
- Снижает барьер входа для пользователей (не нужно вручную вводить данные)
- Повышает точность отслеживания питания
- Улучшает пользовательский опыт за счет AI-технологий

---

## 2. Архитектура и технологический стек

### 2.1 Технологический стек

#### Backend Framework
- **Python 3.12+** — современная версия с улучшенной производительностью
- **FastAPI 0.122.0** — высокопроизводительный async веб-фреймворк
- **Uvicorn 0.38.0** — ASGI сервер для запуска приложения

#### HTTP и networking
- **httpx 0.28.1** — асинхронный HTTP-клиент для вызовов OpenRouter API
- **httpcore 1.0.9** — низкоуровневый HTTP транспорт

#### Валидация данных
- **Pydantic 2.12.5** — мощная библиотека для валидации и сериализации данных
- **pydantic-settings 2.7.1** — управление конфигурацией через переменные окружения

#### Инфраструктура
- **Docker** — контейнеризация приложения
- **Docker Compose** — оркестрация контейнеров
- **GitHub Actions** — CI/CD автоматизация

### 2.2 Архитектурные принципы

1. **Stateless микросервис** — без базы данных, все данные в памяти на время запроса
2. **Асинхронная обработка** — использование async/await для максимальной производительности
3. **Строгая типизация** — все данные валидируются через Pydantic-схемы
4. **Separation of Concerns** — четкое разделение ответственности между модулями
5. **Security by design** — встроенная безопасность на всех уровнях

### 2.3 Схема работы системы

```
[Django Backend (RU Server)]
         ↓
    [Tailscale VPN]
         ↓
[EatFit24 AI Proxy (NL Server)]
    ├── Authentication (API Key)
    ├── Image Validation
    ├── Image Processing
    └── OpenRouter API Call
         ↓
    [GPT-5 Image Mini AI Model]
         ↓
    [Structured JSON Response]
         ↓
[Return КБЖУ to Django Backend]
```

---

## 3. Структура проекта

### 3.1 Файловая структура

```
eatfit24-ai-proxy/
│
├── app/                          # Основной код приложения
│   ├── __init__.py              # Инициализация пакета
│   ├── main.py                  # Входная точка FastAPI, роуты
│   ├── schemas.py               # Pydantic модели запросов/ответов
│   ├── openrouter_client.py     # Клиент для работы с OpenRouter API
│   ├── config.py                # Конфигурация и настройки
│   └── auth.py                  # Аутентификация через API key
│
├── .github/
│   └── workflows/
│       └── deploy.yml           # GitHub Actions для автодеплоя
│
├── docs/                         # Дополнительная документация
│
├── venv/                         # Виртуальное окружение (не в git)
│
├── .env                          # Переменные окружения (не в git)
├── .env.example                 # Пример конфигурации
├── .gitignore                   # Игнорируемые файлы
├── .dockerignore                # Исключения для Docker
│
├── Dockerfile                   # Сборка Docker образа
├── docker-compose.yml           # Оркестрация контейнеров
├── deploy.sh                    # Скрипт ручного деплоя
│
├── requirements.txt             # Python зависимости
├── README.md                    # Основная документация
├── API_DOCS.md                  # Детальная API документация
├── DEPLOYMENT.md                # Руководство по деплою
├── ROADMAP.md                   # Дорожная карта развития
├── RULES.md                     # Правила разработки
└── MIGRATION_MULTIPART.md       # История миграции на multipart
```

### 3.2 Описание ключевых файлов

#### app/main.py
Центральный модуль приложения. Содержит:
- Инициализацию FastAPI приложения
- Определение эндпоинтов (health check, recognize-food)
- Middleware для логирования запросов
- Валидацию загружаемых файлов
- Обработку ошибок

#### app/schemas.py
Pydantic модели для строгой типизации:
- `FoodItem` — модель одного продукта (название, вес, КБЖУ)
- `TotalNutrition` — суммарные показатели КБЖУ
- `RecognizeFoodResponse` — структура ответа API

#### app/openrouter_client.py
Модуль для работы с OpenRouter API:
- Формирование промпта для AI-модели
- Конвертация изображения в base64 data URL
- Асинхронные HTTP-запросы к OpenRouter
- Парсинг JSON-ответов от AI
- Обработка ошибок API

#### app/config.py
Управление конфигурацией через pydantic-settings:
- Загрузка переменных окружения из .env
- Валидация обязательных параметров
- Предоставление настроек приложению

#### app/auth.py
Аутентификация входящих запросов:
- Проверка X-API-Key заголовка
- Защита эндпоинтов через FastAPI Depends

---

## 4. Основные компоненты системы

### 4.1 FastAPI Application (app/main.py)

#### Инициализация
```python
app = FastAPI(title="EatFit24 AI Proxy")
```

#### Middleware для логирования
Каждый запрос логируется с информацией:
- Метод и путь запроса
- IP-адрес клиента
- Код статуса ответа
- Время обработки в секундах

#### Health Check эндпоинт
```
GET /health
```
Простая проверка работоспособности сервиса. Не требует аутентификации.

#### Основной эндпоинт распознавания
```
POST /api/v1/ai/recognize-food
```

**Процесс обработки:**
1. Аутентификация через API key
2. Валидация типа файла (только JPEG/PNG)
3. Проверка размера файла (макс. 5 МБ)
4. Чтение содержимого файла в память
5. Вызов OpenRouter API
6. Парсинг ответа AI
7. Возврат структурированного JSON

### 4.2 OpenRouter Integration (app/openrouter_client.py)

#### Построение промпта

Система генерирует детальный промпт для AI-модели с учетом локали:

**Русский промпт:**
```
Проанализируй изображение еды и верни JSON-ответ следующей структуры:
{
  "items": [
    {
      "name": "название блюда",
      "grams": вес в граммах,
      "kcal": калории,
      "protein": белки в граммах,
      "fat": жиры в граммах,
      "carbs": углеводы в граммах
    }
  ],
  "model_notes": "любые дополнительные заметки или предупреждения"
}

Важно:
- Определи все продукты, видимые на изображении
- Оцени вес в граммах максимально точно
- Рассчитай пищевую ценность (ккал, белки, жиры, углеводы) для каждого продукта
- Используй стандартные базы данных по питанию
- Суммируй все продукты для получения итогов
- Будь точен и реалистичен с размерами порций
```

#### Обработка изображений

1. **Конвертация в base64:**
   ```python
   b64_image = base64.b64encode(image_bytes).decode("ascii")
   data_url = f"data:{content_type};base64,{b64_image}"
   ```

2. **Формирование запроса к OpenRouter:**
   - Multimodal сообщение (текст + изображение)
   - Использование модели GPT-5 Image Mini
   - Таймаут 60 секунд

3. **Парсинг ответа:**
   - Извлечение JSON из markdown блоков
   - Валидация структуры данных
   - Создание объектов FoodItem
   - Расчет суммарных показателей

### 4.3 Data Models (app/schemas.py)

#### FoodItem
```python
class FoodItem(BaseModel):
    name: str              # Название продукта
    grams: float          # Вес в граммах
    kcal: float           # Калории
    protein: float        # Белки (г)
    fat: float           # Жиры (г)
    carbs: float         # Углеводы (г)
```

#### TotalNutrition
```python
class TotalNutrition(BaseModel):
    kcal: float          # Суммарные калории
    protein: float       # Суммарные белки
    fat: float          # Суммарные жиры
    carbs: float        # Суммарные углеводы
```

#### RecognizeFoodResponse
```python
class RecognizeFoodResponse(BaseModel):
    items: List[FoodItem]              # Список продуктов
    total: TotalNutrition              # Суммарные показатели
    model_notes: Optional[str] = None  # Заметки от AI
```

### 4.4 Configuration Management (app/config.py)

Управление настройками через переменные окружения:

```python
class Settings(BaseSettings):
    openrouter_api_key: str
    openrouter_model: str
    openrouter_base_url: str
    api_proxy_secret: str
    max_image_size_bytes: int

    class Config:
        env_file = ".env"
```

---

## 5. API документация

### 5.1 Базовые URL

- **Production:** `http://100.84.210.65:8001` (только через Tailscale VPN)
- **Development:** `http://localhost:8001`

### 5.2 Аутентификация

Все эндпоинты (кроме `/health`) требуют аутентификации через заголовок:

```
X-API-Key: <ваш_api_ключ>
```

### 5.3 Эндпоинты

#### GET /health

**Описание:** Проверка работоспособности сервиса

**Аутентификация:** Не требуется

**Ответ:**
```json
{
  "status": "ok"
}
```

#### POST /api/v1/ai/recognize-food

**Описание:** Распознавание еды на изображении

**Аутентификация:** Требуется (X-API-Key)

**Content-Type:** `multipart/form-data`

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `image` | File | Да | Файл изображения (JPEG/PNG, макс. 5 МБ) |
| `user_comment` | String | Нет | Комментарий пользователя о блюде |
| `locale` | String | Нет | Язык ответа ("ru" или "en"), по умолчанию "ru" |

**Пример запроса (cURL):**
```bash
curl -X POST http://100.84.210.65:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: your_api_key_here" \
  -F "image=@/path/to/food-photo.jpg" \
  -F "user_comment=Гречка с курицей" \
  -F "locale=ru"
```

**Успешный ответ (200 OK):**
```json
{
  "items": [
    {
      "name": "Гречка отварная",
      "grams": 150.0,
      "kcal": 165.0,
      "protein": 6.0,
      "fat": 1.5,
      "carbs": 30.0
    },
    {
      "name": "Куриная грудка",
      "grams": 100.0,
      "kcal": 165.0,
      "protein": 31.0,
      "fat": 3.6,
      "carbs": 0.0
    }
  ],
  "total": {
    "kcal": 330.0,
    "protein": 37.0,
    "fat": 5.1,
    "carbs": 30.0
  },
  "model_notes": "Распознано на основе изображения и комментария пользователя"
}
```

### 5.4 Коды ошибок

| Код | Описание | Пример |
|-----|----------|--------|
| 400 | Некорректный тип файла или пустой файл | `{"detail": "Unsupported file type: image/gif"}` |
| 401 | Неверный или отсутствующий API ключ | `{"detail": "Invalid or missing API key"}` |
| 413 | Файл превышает максимальный размер | `{"detail": "File too large. Max allowed size is 5242880 bytes"}` |
| 422 | Отсутствуют обязательные поля | `{"detail": [{"loc": ["body", "image"], "msg": "field required"}]}` |
| 500 | Ошибка AI сервиса или сервера | `{"detail": "AI service error: <message>"}` |

### 5.5 Интеграция с Django

#### Пример функции распознавания
```python
import requests

API_URL = "http://100.84.210.65:8001"
API_KEY = "your_api_key_here"

def recognize_food(image_path: str, user_comment: str = None, locale: str = "ru"):
    """
    Вызов AI Proxy для распознавания еды

    Args:
        image_path: Путь к файлу изображения
        user_comment: Опциональный комментарий о блюде
        locale: Код языка (по умолчанию "ru")

    Returns:
        dict: Ответ с распознанными продуктами и итогами
    """
    headers = {"X-API-Key": API_KEY}

    files = {"image": open(image_path, "rb")}
    data = {"locale": locale}

    if user_comment:
        data["user_comment"] = user_comment

    response = requests.post(
        f"{API_URL}/api/v1/ai/recognize-food",
        files=files,
        data=data,
        headers=headers,
        timeout=60
    )

    response.raise_for_status()
    return response.json()
```

#### Django View пример
```python
from django.http import JsonResponse
from django.views.decorators.http import require_http_methods
import requests

@require_http_methods(["POST"])
def analyze_food(request):
    """Django view для обработки загрузки и анализа еды"""

    if 'image' not in request.FILES:
        return JsonResponse({"error": "No image file provided"}, status=400)

    image_file = request.FILES['image']
    user_comment = request.POST.get('user_comment', '')
    locale = request.POST.get('locale', 'ru')

    headers = {"X-API-Key": API_KEY}
    files = {"image": (image_file.name, image_file.read(), image_file.content_type)}
    data = {"locale": locale}

    if user_comment:
        data["user_comment"] = user_comment

    try:
        response = requests.post(
            f"{API_URL}/api/v1/ai/recognize-food",
            files=files,
            data=data,
            headers=headers,
            timeout=60
        )
        response.raise_for_status()
        return JsonResponse(response.json())

    except requests.exceptions.HTTPError as e:
        return JsonResponse({
            "error": f"AI Proxy error: {e.response.status_code}",
            "detail": e.response.text
        }, status=e.response.status_code)
```

---

## 6. Безопасность и аутентификация

### 6.1 Многоуровневая защита

#### Уровень 1: Сетевая изоляция (Tailscale VPN)
- Сервис доступен только в сети Tailscale (100.0.0.0/8)
- Внешний доступ полностью заблокирован через firewall
- Все запросы проходят через зашифрованный VPN-туннель

#### Уровень 2: API Key Authentication
```python
async def verify_api_key(x_api_key: str = Header(..., alias="X-API-Key")):
    """Проверка API ключа в заголовке запроса"""
    if x_api_key != settings.api_proxy_secret:
        raise HTTPException(
            status_code=401,
            detail="Invalid or missing API key"
        )
    return x_api_key
```

Защита применяется через FastAPI Depends:
```python
@app.post("/api/v1/ai/recognize-food")
async def recognize_food(
    ...,
    api_key: str = Depends(verify_api_key)
):
    ...
```

#### Уровень 3: Валидация входных данных

**Проверка типа файла:**
```python
ALLOWED_CONTENT_TYPES = {"image/jpeg", "image/png", "image/jpg"}

if image.content_type not in ALLOWED_CONTENT_TYPES:
    raise HTTPException(status_code=400, detail="Unsupported file type")
```

**Ограничение размера:**
```python
if file_size > settings.max_image_size_bytes:  # 5 MB
    raise HTTPException(status_code=413, detail="File too large")
```

**Проверка на пустые файлы:**
```python
if file_size == 0:
    raise HTTPException(status_code=400, detail="Empty file")
```

### 6.2 Логирование и мониторинг

Все запросы логируются с информацией:
```python
logger.info(
    f"Request: {request.method} {request.url.path} "
    f"from {request.client.host}"
)
logger.info(
    f"Response: {request.method} {request.url.path} - "
    f"Status: {response.status_code} - Duration: {duration:.3f}s"
)
```

**Что НЕ логируется (privacy):**
- Содержимое изображений
- Полные тела запросов/ответов
- API ключи

### 6.3 Безопасность секретов

#### Переменные окружения
```bash
# .env файл (не в git!)
OPENROUTER_API_KEY=sk-or-v1-xxxxx
API_PROXY_SECRET=long_random_string_here
```

#### GitHub Secrets
Для CI/CD используются зашифрованные секреты:
- `SSH_PRIVATE_KEY` — приватный SSH ключ
- `SSH_HOST` — IP сервера
- `SSH_USER` — SSH пользователь
- `SSH_PORT` — SSH порт

#### Docker
Секреты передаются через docker-compose:
```yaml
environment:
  - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
  - API_PROXY_SECRET=${API_PROXY_SECRET}
```

---

## 7. Развертывание и DevOps

### 7.1 Контейнеризация (Docker)

#### Dockerfile
```dockerfile
FROM python:3.12-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Install curl for healthcheck
RUN apt-get update && \
    apt-get install -y --no-install-recommends curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY app ./app

EXPOSE 8001

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]
```

**Особенности:**
- Базовый образ `python:3.12-slim` для минимального размера
- Установка curl для healthcheck
- Отключение .pyc файлов для экономии места
- Unbuffered вывод для корректного логирования

#### docker-compose.yml
```yaml
version: '3.8'

services:
  ai-proxy:
    build: .
    container_name: eatfit24-ai-proxy
    ports:
      - "8001:8001"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL}
      - API_PROXY_SECRET=${API_PROXY_SECRET}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

### 7.2 CI/CD через GitHub Actions

#### Workflow триггеры
- **Автоматически:** при push в ветку `master`
- **Вручную:** через кнопку "Run workflow" в GitHub UI

#### Процесс деплоя
```yaml
name: Deploy EatFit24 AI Proxy to NL server

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p $PORT $SSH_HOST >> ~/.ssh/known_hosts

      - name: Deploy to NL server
        run: |
          ssh -p $PORT $SSH_USER@$SSH_HOST << 'EOF'
            cd /opt/eatfit24-ai-proxy
            git pull
            docker compose up -d --build
            docker compose ps
          EOF
```

#### Этапы деплоя:
1. Checkout кода из репозитория
2. Настройка SSH-аутентификации
3. Подключение к серверу через SSH
4. Переход в директорию проекта
5. Получение последних изменений (git pull)
6. Пересборка и перезапуск контейнеров
7. Проверка статуса контейнеров

### 7.3 Инфраструктура

#### Серверная архитектура
```
[GitHub Repository]
        ↓
  [GitHub Actions]
        ↓ (SSH)
[NL Server: 185.171.80.128]
  ├── /opt/eatfit24-ai-proxy/
  ├── Docker Engine
  └── Tailscale VPN (100.84.210.65)
        ↓
[RU Server] (Django Backend)
```

#### Сетевая топология
- **Внешний IP:** 185.171.80.128 (только для SSH)
- **Tailscale IP:** 100.84.210.65 (для API запросов)
- **Порт:** 8001
- **Firewall:** Блокирует все входящие на 8001, кроме Tailscale

### 7.4 Мониторинг и проверка работоспособности

#### Health Check
```bash
# Проверка доступности
curl http://100.84.210.65:8001/health

# Ожидаемый ответ:
{"status":"ok"}
```

#### Проверка логов
```bash
# Логи контейнера
docker logs eatfit24-ai-proxy

# Последние 100 строк
docker logs eatfit24-ai-proxy --tail 100

# С отслеживанием в реальном времени
docker logs -f eatfit24-ai-proxy
```

#### Статус контейнера
```bash
# Список запущенных контейнеров
docker compose ps

# Детальная информация
docker inspect eatfit24-ai-proxy
```

---

## 8. Настройка и конфигурация

### 8.1 Переменные окружения

#### .env.example (шаблон)
```bash
# OpenRouter API settings
OPENROUTER_API_KEY=your_key_here
OPENROUTER_MODEL=openai/gpt-5-image-mini
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Security
API_PROXY_SECRET=changeme_long_random_string

# Application settings (optional)
APP_NAME=EatFit24 AI Proxy
LOG_LEVEL=INFO
MAX_IMAGE_SIZE_BYTES=5242880  # 5 MB
```

#### Описание переменных

| Переменная | Тип | Обязательная | Описание |
|------------|-----|--------------|----------|
| `OPENROUTER_API_KEY` | String | Да | API ключ для OpenRouter |
| `OPENROUTER_MODEL` | String | Да | Название AI модели (по умолчанию gpt-5-image-mini) |
| `OPENROUTER_BASE_URL` | String | Да | Base URL для OpenRouter API |
| `API_PROXY_SECRET` | String | Да | Секретный ключ для аутентификации клиентов |
| `MAX_IMAGE_SIZE_BYTES` | Integer | Нет | Максимальный размер файла (по умолчанию 5 МБ) |
| `APP_NAME` | String | Нет | Название приложения |
| `LOG_LEVEL` | String | Нет | Уровень логирования (INFO, DEBUG, WARNING) |

### 8.2 Локальная разработка

#### Шаг 1: Клонирование репозитория
```bash
git clone <repository-url>
cd eatfit24-ai-proxy
```

#### Шаг 2: Создание виртуального окружения
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/macOS
python -m venv venv
source venv/bin/activate
```

#### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

#### Шаг 4: Настройка переменных окружения
```bash
# Копирование шаблона
cp .env.example .env

# Редактирование (добавьте реальные ключи)
nano .env  # или любой другой редактор
```

#### Шаг 5: Запуск приложения
```bash
# Режим разработки с автоперезагрузкой
uvicorn app.main:app --reload --port 8001

# Приложение доступно на http://localhost:8001
```

#### Шаг 6: Тестирование
```bash
# Health check
curl http://localhost:8001/health

# Тест распознавания (с реальным API ключом)
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: your_api_key" \
  -F "image=@test-food.jpg" \
  -F "locale=ru"
```

### 8.3 Запуск через Docker локально

```bash
# Создание .env файла
cp .env.example .env
# Отредактируйте .env и добавьте ключи

# Сборка и запуск
docker compose up -d --build

# Проверка статуса
docker compose ps

# Просмотр логов
docker logs -f eatfit24-ai-proxy

# Остановка
docker compose down
```

### 8.4 Производственное развертывание

#### Предварительные требования
- Ubuntu/Debian сервер
- Docker и Docker Compose установлены
- Tailscale VPN настроен
- SSH доступ настроен

#### Шаги развертывания

**1. Подключение к серверу:**
```bash
ssh root@185.171.80.128
```

**2. Создание директории проекта:**
```bash
mkdir -p /opt/eatfit24-ai-proxy
cd /opt/eatfit24-ai-proxy
```

**3. Клонирование репозитория:**
```bash
git clone <repository-url> .
```

**4. Настройка .env:**
```bash
cp .env.example .env
nano .env  # Добавьте production ключи
```

**5. Запуск сервиса:**
```bash
docker compose up -d --build
```

**6. Настройка автозапуска:**
Docker Compose автоматически перезапустит контейнер при перезагрузке благодаря `restart: unless-stopped`

**7. Настройка firewall:**
```bash
# Разрешить SSH
ufw allow 22/tcp

# Блокировать прямой доступ к 8001 (доступ только через Tailscale)
ufw deny 8001/tcp

# Включить firewall
ufw enable
```

---

## 9. Процесс разработки

### 9.1 Правила разработки

#### Стиль кода (PEP8)
- Имена переменных понятные, без сокращений
- Строгие аннотации типов (`def func(x: int) -> str`)
- Максимальная длина строки: 100 символов
- Импорты сгруппированы и отсортированы

#### Архитектурные принципы
- **Один модуль — одна ответственность**
- **Явное лучше неявного**
- **Не повторяй себя (DRY)**
- **Обработка ошибок через исключения**

#### Документация
- Docstrings для всех публичных функций
- Комментарии только для неочевидной логики
- README обновляется при изменении API

### 9.2 Git workflow

#### Структура коммитов
```
<type>: <short description>

<optional detailed description>
```

**Типы коммитов:**
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `refactor:` — рефакторинг без изменения функциональности
- `test:` — добавление тестов
- `chore:` — технические изменения (зависимости, конфиг)

**Примеры:**
```
feat: Add user_comment support to recognize-food endpoint

fix: Handle empty files in image upload

docs: Update API_DOCS.md with multipart examples

refactor: Extract prompt building to separate function
```

#### Ветки
- `master` — production-ready код
- `develop` — integration ветка для фич (если используется)
- `feature/*` — ветки для новых фич
- `hotfix/*` — срочные исправления для production

#### Процесс внесения изменений

1. **Обновить ROADMAP.md** — отметить текущую задачу
2. **Создать ветку** (опционально для больших изменений)
3. **Внести изменения в код**
4. **Обновить схемы** (если меняется API)
5. **Тестировать локально**
6. **Обновить документацию**
7. **Создать коммит с понятным сообщением**
8. **Запушить в master** (или создать PR)

### 9.3 Тестирование

#### Ручное тестирование через curl

**Health check:**
```bash
curl http://localhost:8001/health
```

**Успешный запрос:**
```bash
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: test_key" \
  -F "image=@test.jpg" \
  -F "user_comment=Салат цезарь" \
  -F "locale=ru"
```

**Тест ошибок:**
```bash
# Неверный API key
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: wrong_key" \
  -F "image=@test.jpg"

# Неподдерживаемый формат
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: test_key" \
  -F "image=@test.gif"

# Слишком большой файл
curl -X POST http://localhost:8001/api/v1/ai/recognize-food \
  -H "X-API-Key: test_key" \
  -F "image=@huge_image.jpg"
```

#### Тестирование через Python
```python
import requests

response = requests.post(
    "http://localhost:8001/api/v1/ai/recognize-food",
    headers={"X-API-Key": "test_key"},
    files={"image": open("test.jpg", "rb")},
    data={"locale": "ru", "user_comment": "Тестовое блюдо"}
)

print(f"Status: {response.status_code}")
print(f"Response: {response.json()}")
```

### 9.4 Отладка

#### Просмотр логов приложения
```bash
# Docker
docker logs -f eatfit24-ai-proxy

# Локальный запуск (логи в консоли)
uvicorn app.main:app --reload --log-level debug
```

#### Увеличение уровня логирования
В `.env`:
```bash
LOG_LEVEL=DEBUG
```

#### Проверка переменных окружения
```python
from app.config import settings

print(f"Model: {settings.openrouter_model}")
print(f"Max size: {settings.max_image_size_bytes}")
```

---

## 10. Дорожная карта и будущее развитие

### 10.1 Выполненные этапы

#### ✅ Phase 0 — Skeleton (Завершено)
- Создан проект, виртуальное окружение, базовая структура
- Реализован FastAPI с эндпоинтом `/health`
- Добавлены схемы запроса/ответа
- Создана заглушка `/api/v1/ai/recognize-food`

#### ✅ Phase 1 — OpenRouter Integration (Завершено)
- Добавлен модуль `openrouter_client.py`
- Реализован промпт и парсинг ответа модели
- Подключены переменные окружения
- Создан модуль `config.py` для управления настройками
- Интеграция OpenRouter в эндпоинт `/api/v1/ai/recognize-food`

#### ✅ Phase 2 — Docker & Deploy (Завершено)
- Написан Dockerfile
- Создан docker-compose.yml
- Добавлен .dockerignore для оптимизации сборки
- Добавлен healthcheck в docker-compose.yml
- Протестирована сборка и запуск в Docker
- Выполнен деплой на NL-сервер
- Проверка `/health` из РФ-сервера успешна

#### ✅ Phase 3 — Security & Observability (Завершено)
- Ограничен доступ через Tailscale VPN + firewall
- Реализована аутентификация через API key
- Добавлено детальное логирование запросов и ошибок
- Подготовлена API документация для Django-бэкенда
- Настроен автоматический деплой через GitHub Actions

#### ✅ Phase 3.5 — Multipart Migration (Завершено)
- Миграция с JSON (image_url) на multipart/form-data
- Прямая загрузка файлов вместо URL
- Валидация типа и размера файлов
- Обновлена документация

### 10.2 Текущий этап

#### 🔄 Phase 4 — Optimization (В процессе)

**Запланированные улучшения:**

- [ ] **Тюнинг промпта под точность КБЖУ**
  - A/B тестирование различных вариантов промпта
  - Добавление примеров для Few-Shot Learning
  - Уточнение инструкций по оценке порций

- [ ] **Настройка таймаутов**
  - Тестирование оптимального timeout для различных размеров изображений
  - Добавление retry логики для временных сбоев OpenRouter

- [ ] **Ограничения по размеру фото**
  - Автоматическое сжатие больших изображений
  - Оптимизация качества vs размер
  - Предупреждения для пользователей о размере файла

### 10.3 Будущие этапы

#### Phase 5 — Advanced Features (Планируется)

**Кэширование:**
- Кэш распознанных блюд по хешу изображения
- Redis для хранения результатов
- Уменьшение количества вызовов API
- Экономия на стоимости OpenRouter

**Batch Processing:**
- Обработка нескольких изображений в одном запросе
- Асинхронная очередь задач
- Возврат результатов по мере готовности

**Расширенная аналитика:**
- Сбор метрик использования
- Статистика точности распознавания
- Dashboard для мониторинга

#### Phase 6 — Quality Improvements (Планируется)

**Тестирование:**
- Unit тесты для основных модулей
- Integration тесты для API
- E2E тесты с реальными изображениями
- CI pipeline с автоматическим запуском тестов

**Валидация результатов:**
- Проверка адекватности КБЖУ (не может быть 10000 ккал в салате)
- Сравнение с базой данных продуктов
- Флаги для сомнительных результатов

**Улучшение точности:**
- Fine-tuning промпта на основе feedback
- Использование специализированной модели для nutrition
- Добавление контекста из базы рецептов

#### Phase 7 — Scaling (Будущее)

**Horizontal Scaling:**
- Поддержка нескольких инстансов сервиса
- Load balancer
- Session affinity не требуется (stateless)

**Performance Optimization:**
- Профилирование узких мест
- Оптимизация конвертации изображений
- Connection pooling для httpx

**Multi-region:**
- Деплой в нескольких регионах для latency
- Geo-routing пользователей
- Failover между регионами

### 10.4 Технический долг

**Текущие известные ограничения:**

1. **Отсутствие тестов**
   - Нет автоматизированного тестирования
   - Риск регрессий при изменениях
   - Требуется ручное тестирование каждого деплоя

2. **Простая обработка ошибок**
   - Нет retry механизма для OpenRouter
   - Нет graceful degradation при недоступности API
   - Не обрабатываются rate limits

3. **Отсутствие метрик**
   - Нет мониторинга производительности
   - Нет alerting при проблемах
   - Сложно отслеживать SLA

4. **Документация API**
   - Нет автогенерируемой Swagger/OpenAPI документации
   - Требуется ручное обновление при изменениях
   - Нет примеров для всех edge cases

### 10.5 Метрики успеха

**KPI для оценки качества сервиса:**

- **Uptime:** > 99.5%
- **Response Time (p95):** < 10 секунд
- **Error Rate:** < 1%
- **API Cost:** < $0.10 на запрос (OpenRouter)
- **User Satisfaction:** оценка точности КБЖУ от пользователей

**Мониторинг:**
- Количество запросов в день
- Средний размер загружаемых изображений
- Распределение языков (ru vs en)
- Топ распознаваемых продуктов

---

## Заключение

**EatFit24 AI Proxy** — это профессионально разработанный микросервис, который решает критическую задачу автоматизации подсчета калорий в фитнес-приложении. Проект демонстрирует лучшие практики современной разработки:

✅ **Четкая архитектура** — разделение ответственности, модульность
✅ **Безопасность first** — VPN, API keys, валидация входных данных
✅ **DevOps автоматизация** — Docker, CI/CD через GitHub Actions
✅ **Документация** — подробное описание API, деплоя, разработки
✅ **Production-ready** — логирование, мониторинг, error handling

Сервис активно используется в production и готов к дальнейшему масштабированию и развитию.

---

**Версия документа:** 1.0
**Дата:** 30 ноября 2025
**Автор:** Nicolas (EatFit24 Team)
