# Telegram Bot - AI Fitness Lead Magnet

Telegram бот для сбора анкет пользователей и генерации персонализированных планов тренировок с помощью AI.

## 🚀 Быстрый старт

### Локальный запуск

1. **Установите зависимости:**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

2. **Настройте окружение:**
```bash
cp .env.example .env
# Отредактируйте .env и заполните необходимые переменные
```

3. **Примените миграции базы данных:**
```bash
alembic upgrade head
```

4. **Запустите бота:**
```bash
python main.py
```

### Запуск через Docker

1. **Соберите образ:**
```bash
docker build -t fitness-bot .
```

2. **Запустите контейнер:**
```bash
docker run --env-file .env fitness-bot
```

## 📁 Структура проекта

```
bot/
├── app/                       # Основной код приложения
│   ├── config.py             # Конфигурация из .env
│   ├── handlers/             # Обработчики сообщений
│   ├── keyboards/            # Клавиатуры для Telegram
│   ├── middlewares/          # Middleware для обработки
│   ├── services/             # Бизнес-логика
│   │   ├── ai/              # Интеграция с OpenRouter AI
│   │   └── database/        # Работа с БД
│   ├── states/              # FSM состояния
│   ├── texts/               # Текстовые сообщения
│   ├── utils/               # Вспомогательные утилиты
│   ├── validators/          # Валидаторы данных
│   ├── models/              # SQLAlchemy модели
│   ├── constants/           # Константы приложения
│   ├── prompts/             # Промпты для AI
│   └── assets/              # Статические ресурсы (изображения)
├── alembic/                  # Миграции базы данных
├── tests/                    # Тесты
├── main.py                   # Точка входа
├── requirements.txt          # Python зависимости
├── Dockerfile               # Docker образ
├── .env.example             # Пример переменных окружения
└── alembic.ini              # Конфигурация Alembic

```

## 🔧 Зависимости

- **Python:** 3.12+
- **aiogram:** 3.x (Telegram Bot Framework)
- **SQLAlchemy:** 2.x (ORM для работы с БД)
- **PostgreSQL:** База данных
- **OpenRouter API:** AI интеграция
- **Redis:** (опционально) для FSM storage в production

## ⚙️ Основные переменные окружения

| Переменная | Описание | Обязательна |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота от @BotFather | ✅ |
| `OPENROUTER_API_KEY` | API ключ OpenRouter | ✅ |
| `DB_HOST` | Хост PostgreSQL | ✅ |
| `DB_PORT` | Порт PostgreSQL (по умолчанию 5432) | ✅ |
| `DB_NAME` | Имя базы данных | ✅ |
| `DB_USER` | Пользователь БД | ✅ |
| `DB_PASSWORD` | Пароль БД | ✅ |
| `DJANGO_API_URL` | URL Django API (опционально) | ❌ |
| `WEB_APP_URL` | URL Telegram Mini App (опционально) | ❌ |

Полный список переменных см. в `.env.example`.

## 🧪 Тестирование

```bash
# Запустить все тесты
pytest

# Запустить конкретный тест
pytest tests/test_specific.py

# С покрытием кода
pytest --cov=app tests/
```

## 📝 Логирование

Логи записываются в `logs/bot.log` (папка создаётся автоматически при запуске в Docker).

Уровень логирования настраивается через `LOG_LEVEL` в `.env`.

## 🤝 Интеграция с Django API

Бот может интегрироваться с Django backend для синхронизации данных пользователей и подписок.

Настройте `DJANGO_API_URL` в `.env` для активации интеграции.

## 🔐 Безопасность

- ⚠️ **НИКОГДА** не коммитьте `.env` файл в git
- Используйте `.env.example` как шаблон
- Все секреты храните только в переменных окружения
- В production используйте валидацию входных данных

## 📚 Дополнительная информация

- Документация aiogram: https://docs.aiogram.dev/
- OpenRouter API: https://openrouter.ai/docs
